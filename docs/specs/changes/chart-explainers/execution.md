# Execution Log — Changes / Chart Explainers

## Document Control

- **Spec:** `docs/specs/changes/chart-explainers` (id `2026-08-chart-explainers`)
- **Approval mode:** gated · **Depth:** Standard
- **Budget (design §12):** 4 tasks · ≈ 950 LOC · 2 review rounds
- **Leader:** Claude (Fable 5, T1) · **Implementer:** `akili-implementer` (sonnet, T2) · **Reviewer:** `akili-reviewer` (opus, T3, read-only)
- **Run mode:** user instruction "fast and efficient" → single Implementer + single Reviewer per task, lens-checklist review mode, no parallel panel unless the effort dial forces it.
- **Tree state at start (2026-08-24):** worktree branch `bilateral-visual-improvements` at `7bd5acb3`; **uncommitted, foreign changes present** (deletion of `executive-overview.component.*` + edits to `project-detail.component.{html,ts}`) belonging to `changes/executive-overview-grounded-context` T-08. This spec neither touches nor commits those paths; commits use explicit pathspecs. RB-1 stands: T-02 does not start until that spec has landed or the owner waives.
- **Created:** 2026-08-24

## Task Execution History

> **RB-1 waived by owner (2026-08-24):** user instructed "waive RB-1 for T-02" — T-02 may start on Reviewer PASS of T-01 without waiting for `executive-overview-grounded-context` to land. Risk accepted: same-file merge with that spec's pending tasks on `project-dashboard.component.*`; mitigation = pathspec commits, no touching foreign uncommitted changes.

## T-01 — `chart-explainer` shared component + `ChartExplainerService` — rework loop in progress

### Attempt 1 — FAIL (2026-08-24)

- **Implementer (sonnet, medium):** 8 new files (`chart-explainer.component.{ts,html,scss,spec.ts}`, `chart-explainer.service.{ts,spec.ts}`, `chart-explainer.interface.ts`, `chart-explainers.constants.ts` skeleton). 11/11 targeted jest green (`--coverage=false`); all 6 mandated K-004 reds observed with verbatim lines + a bonus fail-closed red; hex grep clean; `npm run build` green; spec-tsc 938 = 938 baseline (first pass 939 — bare `{} satisfies` lost the index signature; fixed with explicit `Record` annotation); eslint 0 errors. Documented interpretation: every close path calls the component's own synchronous `hide(returnFocus)`; PrimeNG `(onHide)` wired only as idempotent backstop (no jsdom-safe way to depend on animation-driven `onHide` timing).
- **Leader dispatch error (recorded):** Reviewer brief's inline diff block arrived empty (K-011 near-miss); corrected by pointing the Reviewer at the validated 603-line diff artifact + working tree. Reviewer audited the working tree files directly — identical content, audit valid.
- **Reviewer (opus, lens checklist): STATUS: FAIL — 4 issues (verbatim summary; full report relayed to attempt 2):**
  1. `surface` disc uses `--ac-white-1` in both themes; dark flip is `#e5e5e5`, glyph `--ac-grey-700` dark `#acacac` → **1.80:1**, below R-CXP-006 AC.3's 3:1 (design §5.1/§5.5 name `--ac-background` for dark = 7.75:1; light on `--ac-background` 3.86:1). Remediation: `var(--ac-background)` single token both themes; fix the SCSS comment.
  2. `(onHide)` backstop never executed by any test (noop-animations ⇒ PrimeNG never emits it; spec's own afterEach strips leftover `.p-popover` nodes) — R-CXP-002 AC.5 undischarged. Remediation: call `onPopoverHide()` directly — (a) open→backstop→closed+focus returned; (b) already-closed→no focus, still closed.
  3. Focus asserted via spy call-counts, not `document.activeElement` (AC.2 names the effect); "no auto-focus into panel on open" clause has no assertion at all. Remediation: add `document.activeElement` asserts + arranged-focus open test (`button.focus()` before `click()`; assert activeElement stays button, panel does not contain it).
  4. AC.4 "exactly one popover in the DOM" proved only by `isOpen()` component flags, no DOM assert. Remediation: assert DOM count, or declare the noop-animation teardown limitation as a KZ-017 blind spot in execution.md and hand the DOM count to T-04 HITL.
- **Reviewer adjudications:** close-path restructure = sound, no behavioral gap (verified ordering in code: `isOpenSignal.set(false)` before `popoverRef().hide()`; displaced chain no-ops on the guard; RB-3 closed by ordering); fail-closed PASS; key-skeleton typing PASS; tokens/a11y surface PASS except issue 1; KZ-015 honored; registry mutation in specs acceptable.
- **Advisories (recorded, never tasks):** (a) RISK — PrimeNG popover root hardcodes `role="dialog"` + `aria-modal` while open, contradicting R-CXP-003's non-modal wording; comes from D-CXP-3's primitive choice, not this diff → put on T-04 VoiceOver checklist, record design note if it announces as dialog. (b) RELIABILITY — no `ngOnDestroy`; destroyed-while-open instance stays in service `openInstance`; T-02's Bars↔Heatmap toggle hits this path (2-line fix suggested). (c) READABILITY — requirements.md says "circular 24 px" vs design/tasks 32 px; implementation followed the authoritative table; fix requirements line at T-04. (d) trailing describe block asserts only toBeTruthy — fold or drop. (e) RESILIENCE — confirm surface disc stays circular at T-04 screenshot. (f) Reviewer scope: build/tsc/jest greens inherited from Implementer (read-only tools), not re-measured.
- **Leader action:** all 4 issues in-scope → attempt 2 dispatched, effort `high` (rework rule + tasks.md a11y bump). Advisory (b) ngOnDestroy is NOT added to scope (advisory never grows the spec); T-02's toggle test will surface it if real.

### Attempt 2 — PASS (2026-08-25)

- **Implementer (sonnet, high), scope: the 4 FAIL issues only.** Changes confined to `chart-explainer.component.scss` (disc token → `var(--ac-background)`, both themes, comment rewritten hex-free) and `chart-explainer.component.spec.ts` (+2 backstop tests calling `onPopoverHide()` directly; `document.activeElement` asserts on Esc + second-activation; new async no-auto-focus test using `jest.spyOn(HTMLElement.prototype,'focus')` + `whenStable()`; DOM-count assert `querySelectorAll('[role="region"]')`==1 pinned to B's panelId; tautological trailing describe dropped). Production `.ts`/`.html`/service byte-identical to attempt 1.
- **K-004 reds (verbatim):** 2a backstop no-op → `Expected: false / Received: true`; 2b guard removed → `Expected number of calls: 0 / Received: 1`; 3 via temporarily-added real focus-into-panel path (tabindex + `(onShow)` handler, fully reverted — Reviewer grep-confirmed zero `onShow|tabindex|onPanelShown|autofocus` in final diff) → `Expected: false / Received: true`; 4 service-defect reintroduced → state-flag asserts red immediately.
- **Honest limitation (declared, Reviewer-verified accurate):** the DOM-count assertion does NOT independently discriminate the "service doesn't hide previous" defect — PrimeNG's own document-click listener redundantly dismisses A; the synchronous `componentA.isOpen()`/focusSpy asserts (which run before any flush) carry that burden. Sanctioned fallback + assertion both delivered.
- **Reviewer (opus): STATUS: PASS.** All 4 issues discharged; production-identical claim proven via unchanged blob hashes; red-harness revert grep-verified; hex gate re-run independently clean; `mock.instances` mechanism verified against jest-mock source; PrimeNG outside-click redundancy verified at `primeng-popover.mjs:249-253`.
- **Verification (Implementer-run):** targeted jest 13/13 green (`--coverage=false`); hex grep clean; `npm run build` complete (pre-existing warnings only); spec-tsc 938 = 938 baseline; bare eslint 0 errors; prettier applied then re-verified green.
- **New advisories (recorded, never tasks):** (a) prototype focus spy restored only by trailing `mockRestore()` — an assertion throw leaves it patched for the file; `afterEach(() => jest.restoreAllMocks())` would fix. (b) **For T-02/T-03:** the no-auto-focus test falsifies component-side focus only; if any `[autofocus]` element ever lands in the popover panel, PrimeNG focuses it via a 5 ms `setTimeout` outside Angular which `whenStable()` does not await — the test would stay green through that regression. Do not put `[autofocus]` content in the panel. (c) re-assert focusSpyA after the flush to catch a late refocus. (d) `mock.contexts` is the documented twin of `mock.instances`.
- **Unmeasured at this task (declared):** full client suite — T-04 carries the full-suite gate ("Leader runs, no concurrent workers"), and a concurrent session is actively editing this checkout (foreign uncommitted changes on `project-dashboard.component.*`), so a full-suite run now would be a wrong measurement, not a slow one (root CLAUDE.md §4.3). Coverage floors likewise T-04.
- **Requirements covered:** R-CXP-002 (AC.1–5 + both scenarios), R-CXP-003 AC.1, R-CXP-006 AC.1 (grep-level; visual at T-04), NFR-CXP-001 (automated part).
- **Budget note:** T-01 consumed 1 rework attempt and 2 Reviewer verdicts. Design §12 budgets "2 review rounds" spec-wide (round 1 code, round 2 copy) — under a strict reading the code phase has now used its round plus one rework; flagged at the Step-5 gate for the owner.
- **Final status: T-01 PASS.**

> **Step-5 gate after T-01 (2026-08-25):** owner chose (a) **start T-02 now** despite the concurrent session's uncommitted edits on `project-dashboard.component.{html,ts}` (risk accepted; never revert/commit foreign changes), and (b) **budget read as 2 review rounds per phase-PR** (code / copy) — T-01's 2 verdicts sit within the code phase; escalation recorded, no re-spec.

## Pivot Record: T-02 (2026-08-25)

- **Trigger:** owner-directed design change mid-T-02 (not a discovered spec flaw): *"adicionar el ? por sección y no por cada gráfica, ejemplo a nivel de Act 3"*. Options presented (per-section / hybrid / keep per-chart); owner chose **per-section only**.
- **Blocker for the approved design:** the approved per-chart pattern places 33 `?` buttons; design RISK-3 already flagged density on the 20-chart deep-dive grid. Owner judged the per-chart noise unacceptable and the section level ("Act") the right explanatory unit.
- **Revised direction:** 6 explainers, one per Act section header (`project-dashboard.component.html` sections `act-1`…`act-6`), keys `act-1-identity` … `act-6-depth`. `chart-explainer` component + service (T-01, committed 5fcc730b) reused unchanged with `placement="inline"` beside each `h2`. Each `<section>` carries `aria-describedby` to the explainer's always-rendered sr-only description. **Dropped:** `viz-chart` `explainerKey`/`describedBy` inputs, `project-dashboard-card` input, per-chart wiring in the 6 other hosts, the 38-key registry. Registry shrinks to 6 entries; completeness test scans only the dashboard template. Budget re-estimated ≈ 400 LOC total (was ≈ 950).
- **ADR impact:** none in the TRD (client-only pattern). Design decisions D-CXP-2 (two placements) and D-CXP-6/D-CXP-8 (viz-chart inputs / template scan across 7 hosts) are superseded in scope — recorded in design.md §11 as D-CXP-10 rather than edited in place.
- **T-01 asset status:** fully reusable; `surface` placement code remains in the component (unused for now — legal, tested, zero-cost).
- **Correction-closure sweep owed** after doc edits: grep `38`, `33 surface`, `explainerKey`, `describedBy`, `EXPECTED_DYNAMIC_KEYS`, `viz-chart` across the spec folder (forward) and grep references to §5.2/§5.3 (backward).
- **Approval:** direction approved by owner at the AskUserQuestion gate 2026-08-25; amended docs to be confirmed at the next gate before T-02 resumes.
> **Owner pre-approval (2026-08-25):** "aprueba los docs y continua con T-02" — the amended requirements/design/tasks are approved in advance; the Leader performs a consistency check on delivery (Pivot direction vs doc content vs sweep results) and T-02 (re-scoped) dispatches immediately on a clean check, no further gate. Any inconsistency found still stops for the owner.
> **Crossed-messages incident (2026-08-25, recorded per K-009 family):** the Implementer delivered the ORIGINAL per-chart T-02 (38 keys, viz-chart inputs, 7 hosts wired, 339/339 targeted green, all mandated reds observed incl. the build-gate `not-a-key` NG2 error) AFTER the pivot was approved — the Leader's ping/pause/pivot messages queued during its long work turn and were not processed before delivery. Not a work failure: the delivery was clean against the order it held. Disposition: work SUPERSEDED by the owner's section-level decision; full safety patch saved at scratchpad `t02-per-chart-superseded.patch` (5,702 lines) before any revert; per-chart evidence (counts 31/20/38 re-verified, reds verbatim, hero-toggle stub blind spot) retained in the teammate report for the record. The foreign session committed its edits (d48ca945 etc.), simplifying the revert. Pivot instructions re-issued consolidated.
> **Pivot docs delivered + Leader consistency check PASS (2026-08-25):** per-chart code fully reverted (tree clean, `project-dashboard.component.spec.ts` back to 183/183, build green); requirements/design/tasks rewritten to the 6-key section design; D-CXP-1…9 preserved, D-CXP-10 added; both correction sweeps run and reported (forward remnant caught & fixed in R-CXP-006 AC.2; backward clean; §10 remnant fixed). `proposal.md` deliberately untouched (intake history). Leader clarification for the record: the "mysteriously staged" completeness spec the Implementer found in the index was the Leader's own `git add -N` (intent-to-add for diff extraction) — no anomaly. Owner pre-approval consumed; T-02 (re-scoped, effort high) dispatches now.
> **Runtime failure (2026-08-25, recorded per K-009):** the Implementer agent died on the account session limit (resets 3:10am Bogotá) before writing any T-02 (re-scoped) code — tree verified clean at the failure point. Not a work FAIL; no attempt consumed. Re-dispatched on limit reset with the same brief (attempt 1, effort high).
## T-02 (re-scoped) — Registry (6 keys) + wiring of the 6 Act headers — rework loop in progress

### Attempt 1 — FAIL (2026-08-25)

- **Implementer (sonnet, high):** 6-literal union + 6 TODO entries with real `derivedFrom`; 6 explainers wired per design §5.3 (inside each h2 flex row, `<span class="self-center">` wrapper — correctly reasoned around `:host{display:contents}`); `aria-describedby` via `viewChild()`+`computed()` pairs (NG9-safe); completeness spec both directions; 193/193 targeted green; all 6 K-004 reds observed verbatim (completeness ×2, build-gate NG2 at html:13:64, loading gate, hardcoded describedby, duplicate 6→7); build green, spec-tsc 938=baseline, eslint 0/0.
- **Reviewer (opus, lens checklist): STATUS: FAIL — 2 issues.** Wiring itself verified correct and complete (keys/anchors/conditionals match §5.3 one-for-one; untouched-files claim holds).
  1. Whole-file prettier reformat of the 3 pre-existing dashboard files: real change ≈240 LOC, diff 2,679 lines — violates T-02 acceptance "unchanged except additive tests" and disarms design §12's LOC tripwire (T-04 measures `git diff --stat`). Voluntary (`husky` pre-commit empty, no lint-staged, no format script). Remediation: (a) restore formatting + re-apply semantic edits, or (b) separate formatting-only commit + record true LOC.
  2. `aria-describedby` asserted for ONE Act, by presence only — misrouting invisible (copy-paste mis-pairing of the 6 viewChild pairs is the dominant defect class; Reviewer hand-verified all 6 correct today). R-CXP-003 AC.2 (equality per Act), AC.3 (conditional Act rendered+linked), `.sr-only` clause, and the loading→loaded linkage transition all unasserted. Remediation: table-driven all-six equality test + `.sr-only` assert + Act-3-rendered case + re-run after loading flip.
- **Advisories (recorded, never tasks):** (a) completeness regex sees only literal `key="…"`, not bound `[key]` — failure mode SAFE (dead-entry test reddens loudly); declared KZ-017 blind spot, the "scan alone is exhaustive" note is exhaustive-for-literals; (b) distinct-keys assert duplicated in two spec files — keep the constants one; (c) T-04 HITL contrast line named `--ac-white-1` for light — STALE since T-01's rework (disc is `--ac-background` both themes) — **Leader fixed the tasks.md line in this same turn**; (d) positive: self-center wrapper + NG9 comment are the right calls.
- **True T-02 size ≈ 240 LOC** (recorded for T-04's budget check — well under the 400 estimate).
- **Leader adjudication:** both issues in-scope → attempt 2, effort xhigh (rework bump). Issue 1: option (a) chosen — restore formatting, re-apply semantic edits (~50 lines across 3 files); keeps the branch's LOC gate meaningful and the shared-conflict surface minimal.

### Attempt 2 — PASS (2026-08-25)

- **Implementer (sonnet, xhigh), scope: the 2 FAIL issues.** (1) `git checkout HEAD --` on the 3 pre-existing dashboard files + ~50 semantic lines re-applied by hand, no prettier on pre-existing files — final `git diff --stat`: 300 insertions / 24 deletions across 5 files (real T-02 ≈ 240–300 LOC, §12 tripwire intact). (2) Table-driven `assertAllSixDescribedByLinkages()`: all 6 Acts, `sr-only` class + textContent EQUALITY vs registry concatenation; run with all 6 rendering (Act 3 via `topsWithPartner` — AC.3 rendered+linked) and again after the loading→loaded flip (KZ-015). Advisory (b) done (duplicate distinct-keys assert removed from dashboard spec).
- **Genuine find during red-observation (Reviewer-endorsed as REQUIRED, not bonus):** the mandated cross-wire mutation did not redden at first — Acts 1/4/5's TODO boilerplate was byte-identical, so the equality gate was satisfiable by three keys (K-004: a gate that cannot go red for its mandated reason is not yet a gate). Fix: every placeholder embeds "(Act N — Name)" after `TODO:` (T-03 lint substring preserved; longest sentence ~155 chars < 220). Mutation re-run → red as intended.
- **K-004 reds (verbatim):** (i) cross-wire act1→act4 → `Expected: "TODO: (Act 1 — Identity)…" / Received: "TODO: (Act 4 — Direction)…"`; (ii) `sr-only`→`not-sr-only-anymore` → `Expected value: "sr-only" / Received object: {"0": "not-sr-only-anymore"}` (mutation on chart-explainer html, reverted, 0-diff confirmed by Reviewer — no hunk for that file); (iii) computed forced null → `toBeTruthy() Received: null` post-transition.
- **Reviewer (opus): STATUS: PASS.** Issue 1 verified in the diff hunk-by-hunk (no formatting-only hunk survives); issue 2 covers AC.2 (all six, equality), AC.3 (conditional rendered+linked), sr-only clause, and the viewChild-timing transition. Untouched-files claim holds. Review rounds: 2 of 2 for this phase, within §12 (per-phase reading).
- **Verification (Implementer-run):** targeted jest 193/193; `npm run build` green; spec-tsc 938 = baseline; eslint 0/0; `grep -c '<h2 id="act-'` = 6; hex grep clean.
- **Advisories (recorded, never tasks):** (a) cross-wiring test's power rests on pairwise-distinct entries — currently incidental; T-03's lint should assert the 6 `what` values pairwise distinct (this is a falsifiable check of R-CXP-001's own "no shared generic copy" clause, already owned by T-03 — not scope growth); (b) completeness regex sees literal `key="…"` only, bound `[key]` invisible — SAFE failure mode, declared KZ-017 blind spot; (c) T-04 HITL contrast line naming `--ac-white-1` — **already fixed by the Leader in tasks.md line 149 before this verdict** (Reviewer's code-only diff couldn't see it); (d) emptyHint branch unexercised until T-03 sets one; self-center visual → T-04 HITL.
- **Requirements covered:** R-CXP-001 (AC.1–3 + both scenarios; AC.4 → T-04), R-CXP-003 AC.2/3/4 (+ scenario both clauses), R-CXP-004 AC.1/2/3 (+ scenario all clauses).
- **Unmeasured at this task (declared):** full client suite + coverage floors → T-04 (Leader runs in isolation); registry lint red on TODO — the lint test does not exist yet (T-03 writes it and starts from that red by construction).
- **Final status: T-02 (re-scoped) PASS.**

