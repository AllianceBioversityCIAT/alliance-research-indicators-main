# Execution Log — Changes / Dashboard Chart Refinements

## 1. Document Control

- **Spec id:** 2026-08-dashboard-chart-refinements
- **Spec path:** docs/specs/changes/dashboard-chart-refinements
- **Approval Mode:** gated — run launched as a background job with the owner's explicit "fast and efficient" instruction; the Leader auto-advances after PASS and stops for HALT / Pivot / budget tripwire / the T-05 HITL visual gate (recorded here per the mode's exception rule).
- **Budget (design §10):** 6 tasks / ~750 LOC / 7 review rounds. Decomposition landed at 5 tasks.
- **Execution order:** T-01 (server) ∥ T-02 → T-03 → T-04 → T-05. Cross-package parallel per root §4.3 (targeted verifications only inside workers; Leader re-measures full suites at T-05).
- **Started:** 2026-08-25

## 2. Task Execution History

### T-02 — Client plumbing: interfaces + Sankey registration — PASS (attempt 1) — 2026-08-25

- **Covers:** R-DCR-005 bundle clause (lazy-chunk placement); enables R-DCR-002.
- **Attempts:** 1 Implementer (`akili-implementer`, effort low, skill `angular-developer`), 1 Reviewer (`akili-reviewer`, checklist mode — ~30 LOC diff under the 50-LOC advisory floor).
- **Files changed (+24/−1):**
  - `client/research-indicators/src/app/shared/interfaces/contract-dashboard.interface.ts` — new `ContractLeverSpFlowLink`, `ContractLeverSpFlows`; `ContractDashboardReport += lever_sp_flows: ContractLeverSpFlows | null`.
  - `client/research-indicators/src/app/shared/components/viz-chart/viz-chart.component.ts` — the three scouted edits only (SankeyChart import, `use([])`, `SankeySeriesOption` in the union). GraphChart deregistration confirmed still deferred (DD-11).
- **Implementer verification:** bare `npx eslint` both files clean; targeted viz-chart spec 17/17 PASS (`--coverage=false`); `npm run build` green (pre-existing warnings only). Chunk-stats gate (the actual R-DCR-005 gate): Sankey present in exactly one of 147 dist chunks (`chunk-65SCKBXH.js`); absent from the three initial-bundle files named by `index.html`; `main-*.js` binds `path:"project-dashboard"` to `import("./chunk-65SCKBXH.js")` (dynamic, lazy). K-004 blindness check recorded (grep proven able to hit). KZ-017 declaration recorded (dist .js + index.html inspected; scss/sourcemaps/server not).
- **Reviewer verdict:** `STATUS: PASS`. Diff is exactly the scouted edits + interfaces, no drift; DTO cross-checked type-for-type against the server's in-flight `ContractLeverSpFlowsDto` (confirms `lever_id: number | null`); chunk evidence asserted on generated `dist/` output per KZ-001/KZ-017; client conventions intact. Reviewer KZ-017 note: verdict rests on Read/Grep + the Implementer's recorded dist evidence (read-only role; no re-measurement beside active workers per root §4.3). K-002 note: spec files are neither linted nor type-checked, so a `ContractDashboardReport` literal inside a `*.spec.ts` could silently omit the new required field — no such site exists today (Implementer grep found none).
- **Requirements covered:** R-DCR-005 bundle clause (lazy-chunk placement evidenced on dist output).
- **Decisions:** `lever_id: number | null` adopted per design §4 "No lever" pseudo-source (Implementer assumption, Reviewer-confirmed against the server DTO).
- **Issues:** none.
- **Gate note:** continue auto-approved (owner's fast-and-efficient instruction; see Document Control).

### T-01 — Server: `lever_sp_flows` sub-report — IN LOOP (attempt 1 FAIL) — 2026-08-25

- **Attempt 1** (Implementer effort medium, skill `nestjs-expert`): DTO + `getLeverSpFlowsReport` + 8th `Promise.allSettled` entry + 11 tests; targeted jest 150/150, controller/service 70/70, eslint clean, build green; K-004 red observed on the 8th-entry wiring ("lever_sp_flows: Cannot read properties of undefined (reading 'map')" ×4 before mocks). KZ-017 disqualifier declared (mocked SQL; owed to T-05 live check). Three documented assumptions — Reviewer adjudicated ALL THREE spec-conformant (lever-agnostic totals forced by R-DCR-001/DD-9/DD-8; aligned-but-lever-less results in no link; JS role mapping). design.md:48 parenthetical "(distinct primary results with a primary lever)" identified as the stale artifact — Leader to correct the doc at finalize (root CLAUDE.md §1).
- **Reviewer verdict attempt 1: FAIL** — 2 issues (full report relayed verbatim to attempt 2):
  1. Unaligned bucket produced by a LEFT-JOIN miss, not a result-level "has no active SP" predicate → a dangling alignment row (active row pointing at deactivated/absent SP) makes an aligned result also emit an Unaligned link, and two dangling rows with different roles emit TWO `(lever, null, null)` links. Violates R-DCR-001 "one link per lever with sp_code: null", glossary "no active SP alignment", DD-9. Fix: two-arm query (aligned INNER-JOIN arm + per-lever unaligned arm via NOT EXISTS matching countQuery's predicate by construction).
  2. Two test descriptions/comments claim falsification power the mocked harness lacks (K-004/KZ-014: red not seen may not be asserted; KZ-017). Fix: pin links-query GROUP BY in SQL-text assertions and/or reword to what the harness proves.
- **Advisories (recorded, non-gating):** DD-9 equality precondition "≤1 active primary lever per result" is not index-backed (unlike the SP side) — added to T-05 live-check expectations; `clarisa_levers` join has no `is_active` filter (consistent with sibling, recorded); no server LIMIT (correct per DD-3, unbounded by construction); design §6 "private" wording vs public method (doc wording); coverage floor not measured on targeted run (Leader re-measures at T-05).
- **Attempt 2** (effort high): two-arm rewrite (leverAlignedLinksQuery INNER-JOIN arm; leverUnalignedLinksQuery NOT-EXISTS arm, GROUP BY lever only), SQL-text assertions extended, mixed-row-set test added (red seen via revert: `Expected length: 1 / Received length: 0` — honestly scoped as call-shape mismatch), 151/151 targeted, eslint clean, build green.
- **Reviewer verdict attempt 2: FAIL** — both attempt-1 fixes confirmed landed, but: (1) same defect class moved — `noLeverUnalignedQuery` still derives "unaligned" from `csp.official_code IS NULL` after a LEFT-JOIN chain (a lever-less result with one resolvable + one unresolvable alignment row counts as aligned in countQuery yet emits a "No lever" link; DD-9 broken; the SQL-text test even pins the defective clause); (2) mixed-row-set test title "regression for the join-miss double-count bug" overclaims the mock's power (KZ-014). Remediation: NOT-EXISTS anti-join in the No-lever query (byte-identical predicate, ideally a shared SQL const), repoint the SQL-text pins, reword two titles. Advisories: design §4 `results_total` parenthetical drift (correct the DOC); no uniqueness on (result_id, is_primary) — DD-9 breakable by data, assert live at T-05; unpinned cross-query predicate identity; unbounded row count (per DD-3); coverage floor measured at T-05.

### T-03 — Sankey rework of `sp-alignment-graph` — IN LOOP (attempt 1 FAIL) — 2026-08-25

- **Attempt 1** (Implementer effort xhigh, skills `angular-developer` + `ui-ux-pro-max`): full in-place rework, 6 files +617/−327; targeted 18/18, project-dashboard 189/189, project-detail folder 365/365, eslint clean, build green (initial 1.12 MB, Sankey in lazy chunk); two K-004 reds observed via revert (cap test, Unaligned-click test). tableModel judgment call (sr-only uncapped aggregate + separate visible anchor table) — ADJUDICATED ACCEPTED by conformance Reviewer (viz-chart's tableModel is plain-text sr-only by construction; design §7.2 to be annotated at finalize).
- **Review mode:** parallel lens reviewers (xhigh task): conformance+reliability, a11y+UX. Both FAIL.
- **Leader adjudication of a reviewer conflict:** conformance lens claimed sankey honors `lineStyle.type` (from `SankeyEdgeStyleOption extends LineStyleOption` in the .d.ts); a11y lens proved from vendored runtime source it does NOT (`SankeyView.js:185` maps link lineStyle through `ITEM_STYLE_KEY_MAP` in `itemStyle.js:45`, which has no `type` key; ribbon is a filled path with zero-width stroke). Runtime evidence wins — DD-4's stated mechanism is factually wrong against echarts as vendored; the design INTENT (role as non-color-alone encoding + truthful legend) is unchanged, so this is rework + a design-doc mechanism correction at finalize, NOT a pivot.
- **Gating issues for attempt 2 (union of both lenses, relayed verbatim):**
  1. (a11y #1) Role encoding is color-alone in the rendered chart; legend mis-describes the graphic. Fix: encoding echarts actually renders (preferred: per-link `lineStyle.borderType/borderWidth/borderColor` — those ARE in ITEM_STYLE_KEY_MAP) + legend updated to match + tests moved onto preserved properties; legibility owed to T-05.
  2. (both lenses) Inert nodes draggable with `move` cursor; per-datum `cursor` is dead data (`SankeySeries.js:185` draggable default; `SankeyView.js:254-275` only cursor write; zrender fallback `pointer`). Fix: `draggable: false` on the series + post-init cursor pass via the `chartInit` instance viz-chart already emits; drop the dead `cursor` field + its assertion; element-level outcome on the T-05 checklist.
  3. (conformance #2) `usesDetailedRows = report() !== null` renders a header-only detailed table when `sps: []` with non-empty flows (the all-unaligned contract). Fix: row-based predicate + fixture (report non-null, sps `[]`, flows all-unaligned → aggregate table renders).
- **Advisories (recorded, non-gating):** parent stub lacks `flows` input (unknown-property diagnostic risk in 189-test suite); `isFlowsError` also matches pre-load state (guard on `data() !== null` suggested); fold splits a visible lever's outflow into a disconnected "Other" pair (attribution nit; info preserved in uncapped tableModel); node identity is display-string (name collision → ambiguous links/cycle rejection; NG0955 on track keys); `UNKNOWN` role label on unaligned rows reads as missing-not-inapplicable; `isEmpty()` branch unreachable in mounted product (parent hides panel first — design note at finalize); double table announcement for screen readers in fallback branch; scroll region without focusable children (keyboard scroll); `navigateToLever` contract_id vs `/project-detail/:id` identifier — T-05 live check; panel stacking collapse risk at 768px — T-05.
- **Attempt 3** (effort xhigh): shared `resolvableActiveSpAlignmentExists` const interpolated into BOTH unaligned arms (byte-identical by construction); `noLeverUnalignedQuery` dropped its three alignment LEFT JOINs for the NOT-EXISTS anti-join; SQL-text pins repointed (defective `csp.official_code IS NULL` pin removed; negative assertions on the dropped LEFT JOINs; sharedPredicateFragment asserted verbatim in both arms); three overclaiming titles reworded; NULL `lever_id` documented unreachable (entity `nullable: false` + DDL `NOT NULL` — Reviewer verified both). K-004 red seen: new pins run against reverted defective query → `Expected substring: "AND NOT EXISTS (" ... 1 failed, 150 passed`; restored → 151/151. eslint clean, build green.
- **Reviewer verdict attempt 3: PASS.** All five prescribed fixes delivered and independently verified (predicate-by-predicate semantic identity to countQuery's CASE; correlation sound; placeholder count safe; title sweep clean; no regressions; `clarisa_lever.id` join confirmed against the shipped sibling).
- **FINAL — Task PASS (3 attempts).** Requirements covered: R-DCR-001 both scenarios all clauses (SQL execution semantics owed to T-05 live check per declared KZ-017 disqualifier — a green here is NOT SQL-correctness evidence).
- **Leader adjudications at finalize (Reviewer-requested bindings):**
  1. Overclaiming code comment softened inline by Leader (Reviewer-prescribed one-line wording fix, no gate re-run; `npx eslint` re-verified clean). Recorded as the one Leader-inline edit of this task.
  2. DD-9 fate DECIDED: accept-and-carry-honestly — multi-primary-lever unaligned results may push `Σ(unaligned links)` above `results_without_alignment`, mirroring §2.1's aligned-side semantics; design.md §2.1 amended with the adjudication; T-05 must verify equality on D514/A631 or attribute the delta to multi-primary rows (aimed query, not hoped-for).
  3. design.md corrections applied by Leader (docs were stale, code is truth — root CLAUDE.md §1): §4 `results_total` parenthetical (lever-agnostic), §6 private→public wording + four-fixed-queries note, §2.1 unaligned-side cardinality amendment.
- **Advisories attempt 3 (recorded, non-gating):** aligned-but-lever-less results appear in no link (mirror of challenge #5 — T-05 to confirm whether the shape exists in real data); four concurrent queries × eight sub-reports — glance at TypeORM pool size before T-05 live run (starvation would present as intermittent sub-report timeout); sharedPredicateFragment assertion proves textual identity, not shared-const usage (future re-inlining stays green).
- **Attempt 2** (effort xhigh): borderType/borderWidth/borderColor role encoding (renderer-preserved properties), `draggable:false`, dead cursor field deleted, chartInit mouseover/mouseout cursor handler, row-based `usesDetailedRows` + empty-sps fixture; 4 K-004 reds seen via Edit-toggling (quoted in report); targeted 22/22, project-dashboard 189/189, eslint clean, build exit 0. Near-miss recorded: Implementer briefly `git checkout --`'d the uncommitted attempt-1 file, restored via pre-saved patch + marker grep; Reviewer independently confirmed nothing lost (all load-bearing markers verified in the diff).
- **Reviewer verdict attempt 2: FAIL** — fixes 1/2(draggable)/3 confirmed correct at renderer source; 2 residual issues: (1) `mouseover` is the wrong zrender hook — Handler.js:95 re-applies the element's `pointer` cursor on EVERY mousemove and only dispatches mouseover on element entry, so the 'default' override survives one frame on inert nodes, and edges are never corrected (the early return on `dataType !== 'node'` was even asserted as intended). Fix: `mousemove` hook + edges fall through to 'default' + repeated-invocation test. (2) Legend swatches claim per-role stroke weights 3/2/1.5px but the option emits flat `borderWidth: 1.5` (comment overclaims "weight"; K-004). Fix preferred: per-role borderWidth 3/2/1.5 matching the kept-verbatim legend (restores DD-4 1:1).
- **Advisories attempt 2 (recorded):** same-hue rim at ~0.70 composited alpha, 0.75px/side — T-05 gets an explicit criterion: three roles distinguishable in a GREYSCALE screenshot, both themes; lever-name collision → silent merge/self-link (name-by-id + label.formatter guard noted for CLARISA vocabulary owner); mixed token idiom in the option builder.
- **Attempt 3** (effort xhigh): `mousemove` hook (edges fall through to 'default'; repeated-invocation test with mockClear between calls) + per-role borderWidth 3/2/1.5 matching the kept legend; K-004 reds quoted (`TypeError ... reading '1'` ×2 for missing mousemove registration; `Expected: 3, Received: 1.5`); targeted 23/23, project-dashboard 189/189, eslint clean, build exit 0. Only component.ts + spec.ts moved vs attempt 2 (html byte-identical — Reviewer verified).
- **Reviewer verdict attempt 3: PASS.** Both fixes verified with the zrender premises re-confirmed at source (Handler.js:95/99/100-101, Displayable.js:310); legend↔option correspondence exact; all regression markers intact; committed test text does not overclaim (the one description reasoning past its red cites a source-confirmed zrender property).
- **FINAL — Task PASS (3 attempts).** Requirements covered: R-DCR-002 all three scenarios (option-shape acceptance + on-screen cursor/legibility owed to T-05 per declared KZ-017 disqualifiers), R-DCR-005 a11y clauses (rendered-focus/contrast owed to T-05 HITL).
- **Leader actions at finalize:** design.md corrected (DD-4 + §7.2 mechanism: lineStyle.type premise falsified at renderer source, encoding moved to borderType/borderWidth/borderColor; §7.2 tableModel two-table split annotation). Attempt-3 advisories recorded: DD-4 doc drift (now closed by this correction); unreachable `leverId != null` branch (by construction, not a gap); comment cites Handler.js:95 where dispatch is :99 (cosmetic); unconditional handler binding on chartInit emission (idempotent today).
- **T-05 checklist additions accumulated from this loop:** inert nodes/links show no pointer/move cursor and are not draggable (real browser); three role weights/patterns distinguishable in a GREYSCALE screenshot, both themes; panel stacking at 768px (table must not collapse to a sliver); contract_id vs `/project-detail/:id` identifier on live data; scroll-region keyboard access + double-table announcement noted for owner judgment.

### Budget tripwire — 2026-08-25

- Design §10 budget (~750 LOC / 7 review rounds) exceeded after T-03: rounds 7 consumed (T-01: 3, T-02: 1, T-03: 3) with two tasks owed; LOC ~1,470 vs ~750. Cause: review-driven hardening within approved scope (T-01 unaligned-SQL two rewrites; T-03 renderer-level encoding substitution after two design premises were falsified at source). No scope creep.
- Escalated to owner with delta and cause; **owner approved CONTINUE** (T-04 + T-05 automated portion, stopping at the T-05 HITL visual gate).

### T-04 — Geo card: value-ramp bars + defect fixes — PASS (attempt 1) — 2026-08-25

- **Covers:** R-DCR-003 (all clauses), R-DCR-004 (both scenarios), R-DCR-005 token clauses.
- **Attempts:** 1 (Implementer effort medium, skills `angular-developer` + `ui-ux-pro-max`; Reviewer full 4R).
- **Files changed (4, 236 diff lines):** `project-dashboard-chart-colors.constants.ts` (+`valueRampColor`, pure, `var(--ac-viz-ramp-N)` by ceil(share×5), guarded edges), `geo-scope-card.component.ts` (swap rank→value ramp at the builder; grid.top 8→44; xAxis splitNumber:3 + hideOverlap:true; labels confirmed position:'right' outside fill), both spec files (+9 tests, 1 corrected with more discriminating assertions).
- **Implementer verification:** targeted 27/27; `tokens:validate` PASS (ramp monotonic both themes); build exit 0 (initial 1.12 MB); eslint clean. Three K-004 reds via Edit-toggle quoted (unconditional stub 6-fail; plausible ramp-3 stub 3-fail proving new tests discriminate; corrected region assertion red). KZ-017 disqualifier declared verbatim.
- **Reviewer verdict: PASS.** §7.3 implemented exactly; value=0→ramp-1 adjudicated conformant (only spec-consistent option; zero bar renders no fill); rank vocabulary fully removed with discriminating negatives covering all 5 rank tokens; one shared builder → all three charts; correct (value) axis; no out-of-scope touches; corrected test GAINED discriminating power (KZ-001); tokens both themes verified at colors.scss.
- **Advisories (recorded → folded into T-05 checklist):** map is continuous visualMap vs bars' discrete 5-step quantization — sample a MID-VALUE country in the visual pass, not only the max; grid.top reserve costs ~20% plot height at default 200px — screenshot the fullest ranking; dark-theme ramp-1 near card background — dark screenshot to confirm value labels carry legibility.
- **Gate note:** continue auto-approved (owner CONTINUE decision post-tripwire).

### T-05 — Validation sweep — IN LOOP (automated portion, attempt 1 FAIL on evidence audit) — 2026-08-25

- **Attempt 1** (worker effort high, skill `systematic-debugging`; evidence audited by Reviewer — no code diff, validation task): server 340 suites/2548 tests green, cov 85.38/74.72/84.95/85.52 (60% floor clear); client 323 suites/6916 tests green, cov 97.78/92.05/97.13/98.19 (40/20/45/30 clear); serial runs, clean scope justified; tokens:validate PASS; builds exit 0; initial 1.12 MB/261.31 kB; chunk check independently re-verified by auditor (Sankey only in chunk-W3EIUB2C.js, absent from all 40 initial files — method STRONGER than T-02's, positive control retires the blind-grep risk). Live: D514 200/0.320s, flows block present first probe; totals 369/4/365 consistent; DD-9 delta 14 EXACTLY explained by 14 dual-primary-lever unaligned results (independent SQL + payload derivations agree); A631 zero-result edge clean; aligned-but-lever-less shape confirmed live (appears in no link — accepted design behavior); read-only discipline held, scripts cleaned up.
- **Evidence auditor verdict attempt 1: FAIL** — (1) the reported sp_alignment cross-check is vacuous: both blocks' count queries are byte-identical (repository.ts:1323-1339 vs :1511-1527), so agreement is by construction and the NEW per-lever link counts had no independent validation — remediate with `top_primary_levers` (same payload, genuinely different SQL: no alignment predicate): per lever, top count == unaligned link + distinct aligned; one-direction caveat (LIMIT 10) declared; (2) multi-SP→multi-link clause got no live instance and no KZ-017 declaration (D514's only multi-SP result is lever-less, contributes zero links) — find a contract carrying the shape or record its absence as a finding. Advisories to fold into the report: per-file coverage rows for touched files (NFR-3 is touched-files); budget-warning status (exit 0 ≠ no warnings); container build provenance; DD-9 probe SQL verbatim + COUNT(DISTINCT lever_id) confirmation; contract_id route question belongs to HITL, not closed by the server echo.
- **Attempt 2** (effort xhigh): both audit FAILs remediated live. (1) `top_primary_levers` cross-check (genuinely different SQL — no alignment tables, no NOT EXISTS): D514 per-lever 360=359+1, 18=18+0, 1=1+0, 1=1+0; all tops levers in flows; LIMIT one-direction caveat declared (cap never bound: 4 rows < 10); naive-sum trap PROVEN live on A511 lever11 (3 aligned links from ONE result — dedicated COUNT(DISTINCT result_id) SELECT was load-bearing, not decoration). (2) Multi-SP→multi-link live-confirmed: discovery SELECT found 10 qualifying rows; A511 result 33344 lever11 × {SP03, SP06, SP13} emits three links count 1 each through the real endpoint — R-DCR-001 multiplicity clause closed on live data. Additions: per-file coverage rows quoted (repository 95.46/68.71/95.91/95.79; sp-alignment-graph 92.37/78.57/96.87/91.00; geo-scope-card 88.23/77.77/92.30/86.11; viz-chart 94.11/81.81/100/94.02; chart-colors 100×4 — note Jest column order is Stmts/Branch/Funcs/Lines, NOT aligned with the client floor phrasing statements/branches/lines/functions; all clear either reading); fresh build: NO initial-bundle budget warning, 6 component-scss warnings all in unrelated pre-existing files; container provenance: bind mount + build-then-run, dist mtime > src mtime AND attempt-3-only identifiers grepped in the running dist; DD-9 probe SQL quoted verbatim with COUNT(DISTINCT lever_id) confirmed, same 14/14 re-derived.
- **Evidence auditor verdict attempt 2: PASS.** D514 ledger re-derived independently — closes exactly on four independent paths (auditor computed the full reconciliation: 366 levered + 3 lever-less = 369; 4 aligned = 1 levered + 3 lever-less). Cross-check confirmed structurally capable of reddening on precisely the LEFT-JOIN-miss class that failed T-01 twice. Advisories adjudicated by Leader: A1 degrade gate → CLOSED by attempt 3 (below); A2 A511 delta → CLOSED by attempt 3 aimed query; A3 missing rows → CLOSED by attempt 3; A4 KZ-014 — the "arithmetic only balances under anti-join" provenance parenthetical is STRUCK from the record (anti-join and join-miss produce identical numbers absent a mixed-alignment row, none shown; primary provenance evidence — dist grep of attempt-3-only identifiers + mtimes — stands alone); A5 KZ-017 — the cross-check's own blind spot recorded: a wrong-and-consistent shared predicate would satisfy both sides; mitigation cited: T-01 attempt-3 Reviewer verified that predicate clause-by-clause against countQuery's CASE.
- **Attempt 3** (effort medium): requirements §7 row 2's mandatory degrade gate closed — new `test/dashboard-lever-sp-flows-degrade.integration-spec.ts` (171 LOC, K-021 scope: controller+service only, repository useValue, DataSource {}), binding the REAL `getContractDashboard` composition over 8 individually-stubbed sub-reports (flows rejects, 7 resolve) — exercises production `Promise.allSettled` + errors[] interpolation, stronger than the template's whole-method stub. Asserts through the real ResponseInterceptor/GlobalExceptions wire: HTTP 200, `lever_sp_flows` null, `errors[]` entry `/^lever_sp_flows: /`, four sibling blocks intact, envelope fields. K-004 red quoted (`Received: null`, 1 failed/1 total, non-vacuous — status assertion sits above the flipped one). `npm run test:integration`: new file PASS; bare eslint clean after fixer. A511 delta of 1 attributed by aimed query: exactly one dual-primary-lever unaligned result (result_id 5649, lever_count 2) — the case the code comment pre-registered, corroboration not coincidence. Coverage rows: project-dashboard.component.ts 95.87/82.93/97.12/96.34; both DTOs 100×4 (import-chain caveat declared: Istanbul counts decorator statements on module load, via the controller's @ApiResponse value-reference).
- **Reviewer verdict attempt 3: PASS.** K-021 clean (providers exactly match the service constructor); bind mechanic sound (no private-member access; `strictBindCallApply: false` wrinkle noted); envelope asserted at the wire; K-004 red adequate and not overclaimed; the three OTHER failing integration suites confirmed pre-existing and unreachable from this diff.
- **AUTOMATED + LIVE PORTION OF T-05: COMPLETE (attempts: 1 FAIL on evidence, 2 PASS, 3 closure PASS).** Task stays `[~]` — the HITL visual pass is the sole outstanding item (Done definition requires visual evidence attached).
- **Pre-existing findings for follow-up OUTSIDE this spec (recorded, not tasked — advisory rule):** `test/agresso-contract-insights.integration-spec.ts` and `test/agresso-contract-indicator-details.integration-spec.ts` fail to compile their TestingModule (missing `BilateralProjectMappingRepository` provider added to the service constructor after they were written); `bilateral-primary-contributing-sp.integration-spec.ts` needs `T13_MYSQL_PASSWORD` in the shell. None caused by this spec.
- **Attempt-3 advisories (recorded):** three of seven sibling blocks unasserted in the degrade spec (residual risk small — identical ternary per sibling); `arrayContaining` permits extra errors entries (toHaveLength(1) would harden); repoStub typing survives only under `strictBindCallApply: false`; committed-bytes lint reading available via git show | eslint --stdin if wanted; stronger-red variant (rethrow-on-first-rejection composition) noted for future re-hardening.
