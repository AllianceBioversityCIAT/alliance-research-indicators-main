# Execution Log — docs/specs/changes/dashboard-refresh

## Document Control
- Lite · owner-approved 2026-08-25 · Leader Claude (Fable 5) · Implementer akili-implementer (sonnet) · Reviewer akili-reviewer (opus)

## Task Execution History

## T-01 — Invalidate dashboard caches on route leave — attempt 1 (2026-08-25)
- **Implementer (impl-t01, sonnet, medium):** `invalidate(contractId?)` added to dashboard + insights services (single-slot adaptation: clears only when the loaded id matches or no id given); `project-detail.component.ts` `ngOnDestroy` invalidates dashboard, insights AND CLARISA (CLARISA's method pre-existed with **no call site anywhere** — a documented prior reviewer finding at `get-clarisa-project.service.ts:28-30`; this task closes it). Reds verbatim: `TypeError: service.invalidate is not a function` (×2 services), component `Expected: "mock-id" / Number of calls: 0`. 79/79 green; spec-tsc 942; eslint clean. 121+/0-.
- **Reviewer (opus): STATUS: PASS (conditional on the build gate, Leader-run).** Verified re-issue at the HTTP layer (`expectOne` after flush, `expectNone` for the other-id clause); `data()` correctly NOT cleared (codebase convention: consumers compare `loadedContractId()` before trusting `data()`); 14 consumer files all inside the project-detail subtree — no longer-lived reader; tab switches don't destroy the parent.
- **Advisories (recorded):** (a) **for T-02:** CLARISA's pre-existing `invalidate` only deletes the memo entry and leaves `loadedContractId`/`data` intact → during `refreshAll()` a convention-following consumer keeps trusting the pre-refresh payload for the in-flight window — decide the in-flight contract in T-02 (Leader ruling: same-contract stale-until-replaced is the intended refresh UX; no clearing, no skeleton); (b) `update()` no-ops when `loadedContractId` is null — ordering matters once T-02 adds Refresh + "Try again"; (c) tasks.md tsc baseline updated 938→942 in T-01/T-02.
- **Gate outstanding:** `npm run build` — deferred until no worker is active in the package (§4.3); commit follows the build.

> **Batch gates (Leader, in isolation, 2026-08-25, both workers idle):** `npm test -- --silent` → 323 suites, **6,934/6,934** green (+39 vs 6,895 pre-batch); `npm run lint -- --quiet` → all files pass; `npm run build` → complete (only the 3 pre-existing warnings; `project-dashboard` chunk 1.22 MB / 311.59 kB). Note: the first two build attempts failed ONLY on Google-Fonts inlining because the Leader shell sandbox blocks Node fetches — compiler step had zero TS/NG errors both times; re-run outside the sandbox succeeded. Not a code signal.
- **T-01 final: PASS** — build gate discharged by the batch build above; committed by pathspec (services + project-detail component + specs).

## T-02 — Refresh button + refreshAll() — rework loop (2026-08-25)
### Attempt 1 — FAIL
- **Implementer (impl-t02, sonnet, medium):** `refreshing` signal, `refreshAll()` (allSettled of 4 sources, try/finally, re-entrancy guard), icon button in Act-1 h2 (`ml-auto`, tokens, 32 px), `disabled`/`aria-busy` bindings; declared deviation: visibility gated on `!!project()` instead of `!loading()` (mid-refresh `loading()` flip would unmount the focused button). Reds verbatim: `expect(button).toBeTruthy() Received: null`, `TypeError: component.refreshAll is not a function` (5 failed). 194/194 green; spec-tsc 942; eslint; hex clean; 179+/0-.
- **Reviewer (opus): STATUS: FAIL — 1 issue.** The deviation's rationale verified true (invalidate doesn't touch `project()`; `project.set(data)` only after await) — but `fetchProjectDetail` sets `project.set(null)` on BOTH failure paths (`:72`, `:82`), so a FAILED refresh unmounts the button permanently (no retry, focus destroyed) — violates NFR-DRF-001 (unqualified "after refresh") and R-DRF-002 AND IT MUST ("initial" skeleton only). Remediation: latch signal `hasEverLoadedProjectDetail` set true on first non-null `project()` and never reset; falsifying test with one rejecting source → button present, enabled, `document.activeElement`. (`!!project() || refreshing()` is NOT sufficient.)
- **Verified by Reviewer:** all 4 sources asserted individually; overview untouched (read the method, not just the spy); KZ-015 busy transition correct; `disabled` covers no-double-load; T-01 `update()` advisory does not bite (force loads repopulate `loadedContractId`).
- **Advisories:** button lacks `self-center` in an `items-baseline` h2 (the sibling explainer has it) — likely misaligned; `ml-auto` on wrap pushes to its own line at narrow widths; no rejected-source test (closed by the remediation test).
- **Leader action:** attempt 2 → impl-t02, effort high; `self-center` folded in as a zero-risk tidy (placement consistency with the adjacent control, not new scope).
### Attempt 2 — in re-review (2026-08-25)
- **Implementer (impl-t02, sonnet, high):** latch `hasEverLoadedProjectDetail` (signal, set by a constructor `effect()` on first non-null `project()`, never reset) → `hasLoadedInitialProjectDetail` computed reads the latch; falsifying failed-refresh test — **red verbatim:** `expect(buttonAfter).toBeTruthy() Received: null`; `self-center` tidy. 196/196 green; spec-tsc 942; eslint; hex clean; cumulative 243+/0-.
- **Reviewer (opus), attempt 2: STATUS: PASS.** Latch one-way (no reset path; `project.set(null)` branches can't unmount the button); effect in constructor (valid injection context; signal writes allowed in Angular 19); timing matches production (first non-null after async load). Cross-contract persistence verified real (child route reuses the instance — sibling `contractId` effect at `:1376` exists for that reason): contract B's initial load shows the button **visible but disabled** (`loading()` true) — within "hidden/disabled" wording; strict-hidden would need a reset in the `contractId` effect (preference, not conformance). `self-center` applied.
- **Reviewer retraction (recorded, KZ-007):** the earlier "corrupted diff artifact" claim was its own Grep rendering dropping `/` characters — confirmed on a working-tree file via Read; the Leader's on-disk verification of the Sankey artifact already showed `//`. Artifact pipeline unaffected; the unreconciled 51+ vs 46+ sub-figure stands independently as an Implementer-report arithmetic slip.
- **Batch build (Leader, outside sandbox, no workers active):** complete, chunk `project-dashboard` 1.22 MB / 311.49 kB; targeted dashboard spec 196/196; full suite 6,934/6,934 ran earlier with T-02 attempt 1 in tree (attempt-2 delta = 1 test + latch; re-run at archive).
- **Budget:** T-02 2 rounds vs 1 (defect: Refresh vanishing after a failed refresh). Total spec ≈ 364 LOC vs ≈120 estimate — Lite design carried no tripwire; recorded.
- **Final status: T-02 PASS → spec complete (T-01, T-02 done).** Owner HITL: visual glance on D514 (button alignment in the Act-1 header; back-navigation freshness; Refresh busy state).
