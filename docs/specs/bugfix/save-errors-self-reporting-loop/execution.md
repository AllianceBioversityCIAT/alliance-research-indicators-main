# Execution Log — docs/specs/bugfix/save-errors-self-reporting-loop

## Document Control
- Lite · owner-approved 2026-08-25 · Leader Claude (Fable 5) · Implementer akili-implementer (sonnet) · Reviewer akili-reviewer (opus)

## Task Execution History

## T-01 — Bypass predicate + null-safe errors — attempt 1 (in review, 2026-08-25)
- **Implementer (impl-t02, sonnet, medium):** regression tests first on a REAL interceptor mount (`provideHttpClient(withInterceptors)` + `HttpTestingController`, `ApiService.saveErrors` as a thin wrapper POSTing through the same HttpClient — the only harness that can observe the recursion, KZ-017). **Reds on HEAD verbatim:** `expect(recursiveReports.length).toBe(0) → Expected: 0 / Received: 1`; `Expected constructor: HttpErrorResponse / Received constructor: TypeError`. Fix: `isErrorReportingRequest()` bypass (DD-1), `error.error?.errors ?? error.message` (DD-2, one site exists — brief's "both" was stale). 28/28 green; eslint clean; spec-tsc 942.
- **Baseline root-caused (KZ-007):** the branch's 938→942 drift includes a pre-existing `TS2769` at `http-error.interceptor.spec.ts(560,48)` (2-arg `new HttpRequest('POST', url)`) in the untouched AI-formalize test. Zero net-new from this fix.
- **Process:** Implementer isolated via a tagged `git stash push -m` + drop (shared-stack rule followed; `git stash list` shows no residue). Build deferred to Leader (sibling worker active).
- **Reviewer (opus): STATUS: FAIL — 1 issue (Leader design defect, DD-1):** `req.url.startsWith(environment.saveErrorsUrl)` with `saveErrorsUrl: ''` (the value `environment.example.ts` ships, per the child guide's documented setup path) is always true → early `return next(req)` bypasses EVERY request: no toasts, no reporting, no slow-request telemetry, silently. Violates R-SEL-001 "must NOT suppress the original request's handling" and the "any environment" goal. Under that config the 25 pre-existing tests would go red — current green depends on the Implementer's untracked `environment.ts`. `undefined` is safe (`startsWith(undefined)` → "undefined"); `''` is the exact dangerous value. Remediation: `(!!saveErrorsUrl && startsWith) || prodHost`, + falsifying test (`saveErrorsUrl=''`, app request fails → `showToast` called; red now). DD-1 amended by the Leader in the same turn.
- **Verified by Reviewer:** recursion fix correct (one report; original `HttpErrorResponse` preserved instead of a `TypeError`); early return precedes `timeoutCheck` construction (no 5 s timer for reporting requests); K-005 letter preserved; harness sound (`environment` restored in `afterEach`, scoped hooks).
- **Advisories:** doc drift "both toast sites/lines 40-75" → corrected to the single site (:85) in requirements/design; add `httpMock.verify()` in the new describe's afterEach; with `saveErrorsUrl: ''` `api.saveErrors()` will POST to `''` on every failure in a default local setup — known state, out of scope (recorded).
- **Leader action:** attempt 2 → impl-t01 (impl-t02 busy on dashboard-refresh T-02; disjoint files), effort high.
- **Attempt 2 (impl-t01, sonnet, high):** guard `(!!saveErrorsUrl && startsWith) || prodHost`; new falsifying test (`saveErrorsUrl = ''` → toast still fires) — **red on attempt-1 code verbatim:** `Expected: {"detail": "boom", "severity": "error", "summary": "Error"} / Number of calls: 0`. `httpMock.verify()` added → exposed two unflushed requests (DD-2 test's own saveErrors POST; the empty-string test's app failure now also reports to URL `''`) — both flushed explicitly. 29/29 green; spec-tsc 942; eslint clean; 196+/6-. In re-review.
- **Reviewer (opus), attempt 2: STATUS: PASS.** Guard verified at source (`:26`) with a comment recording the `undefined`-safe / `''`-dangerous distinction; both `verify()`-driven flushes are assertions (`expectOne`), the second pinning the degenerate state "with `saveErrorsUrl === ''` one failure yields exactly one POST to `''`". Nothing else moved.
- **Residual (recorded, out of scope — owner: whoever next touches `environment.saveErrorsUrl`/`api.service.ts` saveErrors):** with `saveErrorsUrl === ''` the POST to `''` is not a reporting request either, so if THAT POST fails it recurses (original bug, empty-config variant). Real fix: make an unconfigured `saveErrorsUrl` loud (skip reporting + `console.warn` once) rather than merely non-bypassing. Not a defect of this spec's stated scope (Reviewer explicitly declined to move goalposts).
- **Budget:** 2 review rounds vs 1 estimated — the extra round caught a Leader design defect that would have disabled error handling for anyone on the committed env template. Recorded.
- **Final status: T-01 PASS** (build done-check at batch close).

> **Batch gates (Leader, in isolation, 2026-08-25, both workers idle):** `npm test -- --silent` → 323 suites, **6,934/6,934** green (+39 vs 6,895 pre-batch); `npm run lint -- --quiet` → all files pass; `npm run build` → complete (only the 3 pre-existing warnings; `project-dashboard` chunk 1.22 MB / 311.59 kB). Note: the first two build attempts failed ONLY on Google-Fonts inlining because the Leader shell sandbox blocks Node fetches — compiler step had zero TS/NG errors both times; re-run outside the sandbox succeeded. Not a code signal.
