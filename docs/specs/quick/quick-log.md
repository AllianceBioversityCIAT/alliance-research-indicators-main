# Quick Changes Log

One-line record of trivial, fast-tracked changes made with `/akili-quick`.

| Date | Change | Files | Verification | Commit |
|---|---|---|---|---|
| 2026-07-22 | quick/readme-monorepo-scope — reconcile README with the monorepo constitution: client no longer "out of scope", add infrastructure.md + model-routing.md to the docs map, add client child guide, update repo-layout tree | README.md | link targets verified to exist; no scope contradictions remain | [SPEC:quick/readme-monorepo-scope] |
| 2026-08-13 | quick/capdev-email-url-source — add `source=star` to the CapDev bulk-upload email link so it lands on the results that upload created. Slug derived via `ReportingPlatformEnum.STAR.toLowerCase()`, matching the client's own lower-cased-`platform_code` rule rather than a third hard-coded spelling. The `indicatorTab=1` → `indicator`/`contract` half was **already** delivered by T-10 (`ba5a90ee`) of the now-archived `results-center/url-filters`; the reported email came from the deployed test env running older code. D6's twin-literal control extended on the client so it still covers the full string as sent. **Scoping confirmed by the product owner:** the bulk upload never creates results on another platform, so `source=star` describes exactly the set the email is about and hides none of it | `server/…/capdev-bulk-notification.service.ts` (+ `.spec.ts`), `client/…/results-center-url.codec.spec.ts` | server 328 suites / 2,217 tests · client 309 suites / 6,508 tests · server lint clean | [SPEC:quick/capdev-email-url-source] |
| 2026-08-14 | quick/sidebar-optional-after-submit — move the optional "Pool funding alignment" divider + section to render after the Review/Submit/Approve buttons in the result sidebar (was before the buttons) | client/research-indicators/src/app/shared/components/result-sidebar/result-sidebar.component.html | manual structure check (deps not installed locally); existing sidebar spec assertions preserved (divider+row kept siblings in a reused .options-container; array-last + hidden-when-ineligible unchanged); no TS/logic/contract change | [SPEC:quick/sidebar-optional-after-submit] |
| 2026-08-20 | quick/pool-funding-catalog-skeleton — upgrade legacy HLO catalog loading spinner in main page to structured PrimeNG skeleton loaders with design tokens | client/research-indicators/src/app/pages/platform/pages/result/pages/pool-funding-alignment/pool-funding-alignment.component.html, pool-funding-alignment.component.ts | `npx jest` (350/350 passed) & `npx eslint` (0 errors) | [SPEC:quick/pool-funding-catalog-skeleton] |
| 2026-08-20 | quick/pool-funding-loading-copy — update ToC catalog loading banner text to reference PRMS instead of CLARISA | client/.../pool-funding-alignment.component.html, client/.../sp-toc-alignment-block.component.html, client/.../sp-toc-alignment-block.component.spec.ts | `npx jest` (350/350 passed) | [SPEC:quick/pool-funding-loading-copy] |
| 2026-08-20 | quick/pool-funding-sps-skeleton — add animated PrimeNG skeleton loaders for Science Programs section during initial fetch from CLARISA | client/research-indicators/src/app/pages/platform/pages/result/pages/pool-funding-alignment/pool-funding-alignment.component.html, pool-funding-alignment.component.spec.ts | `npx jest` (351/351 passed) & `npx eslint` (0 errors) | [SPEC:quick/pool-funding-sps-skeleton] |
| 2026-08-20 | quick/hide-pool-funding-for-oicr — hide Pool Funding Alignment section in sidebar and redirect if accessed for OICR results (indicator_id === 5) | client/research-indicators/src/app/shared/components/result-sidebar/result-sidebar.component.ts, result-sidebar.component.spec.ts, client/.../pool-funding-alignment.component.ts | `npx jest` (99/99 and 351/351 passed) & `npx eslint` (0 errors) | [SPEC:quick/hide-pool-funding-for-oicr] |
| 2026-08-20 | quick/pool-funding-toc-info-banner — update general info banner and section label to reference Theory of Change results (Outputs, Outcomes) across all levels | client/research-indicators/src/app/pages/platform/pages/result/pages/pool-funding-alignment/pool-funding-alignment.component.ts, pool-funding-alignment.component.html | `npx jest` (351/351 passed) & `npx eslint` (0 errors) | [SPEC:quick/pool-funding-toc-info-banner] |
| 2026-08-20 | quick/pool-funding-alerts-ux-modernization — modernize all informative, warning, error, and read-only banner styles with ARI design tokens and contextual icons | client/research-indicators/src/app/pages/platform/pages/result/pages/pool-funding-alignment/pool-funding-alignment.component.html | `npx jest` (351/351 passed) & `npx eslint` (0 errors) | [SPEC:quick/pool-funding-alerts-ux-modernization] |
| 2026-08-20 | quick/sidebar-prms-sync-button — add PRMS SYNC button below Pool Funding Alignment in result sidebar, enabled when result is approved and pool funding is completed | client/research-indicators/src/app/shared/components/result-sidebar/result-sidebar.component.html, result-sidebar.component.ts, result-sidebar.component.spec.ts | `npx jest` (104/104 passed) & `npx eslint` (0 errors) | [SPEC:quick/sidebar-prms-sync-button] |
| 2026-08-21 | quick/pool-funding-toc-clarified-banner — clarify Pool Funding Alignment main banner copy to explain aligning with SP and contributing to ToC (Outputs, Outcomes) | client/research-indicators/src/app/pages/platform/pages/result/pages/pool-funding-alignment/pool-funding-alignment.component.ts | `npx jest` (351/351 passed) & `npx eslint` (0 errors) | [SPEC:quick/pool-funding-toc-clarified-banner] |
| 2026-08-21 | quick/pool-funding-help-guide-modal — add interactive '?' help button, banner guide link, and visual workflow modal in Pool Funding Alignment | client/research-indicators/src/app/pages/platform/pages/result/pages/pool-funding-alignment/pool-funding-alignment.component.html, pool-funding-alignment.component.ts, pool-funding-alignment.component.spec.ts | `npx jest` (353/353 passed) & `npx eslint` (0 errors) & `ng build` pass | [SPEC:quick/pool-funding-help-guide-modal] |
| 2026-08-21 | quick/pool-funding-help-button-refinement — refine '?' help icon using PrimeIcons pi-question-circle with subtle hover and proportional typography alignment | client/research-indicators/src/app/pages/platform/pages/result/pages/pool-funding-alignment/pool-funding-alignment.component.html | `npx jest` (353/353 passed) & `npx eslint` (0 errors) | [SPEC:quick/pool-funding-help-button-refinement] |










| 2026-09-01 | quick/actors-youth-definition — add the UN youth age-range (15–24) note and source link to the ACTORS callout in Innovation use details | client/research-indicators/src/app/pages/platform/pages/result/pages/innovation-use-details/innovation-use-details.component.html, innovation-use-details.component.ts | `npx eslint` (0 errors) & `npm test -- --testPathPattern innovation-use-details.component.spec` (144/144 passed) | [SPEC:quick/actors-youth-definition] |
| 2026-09-02 | quick/count-placeholder-copy — replace the six numeric "Enter a count" placeholders in Innovation use details (actor sex/age disaggregation + organization count) with the friendlier "How many?" | client/research-indicators/src/app/pages/platform/pages/result/pages/innovation-use-details/components/innovation-use-actor-item/innovation-use-actor-item.component.html, .../innovation-use-organization-item/innovation-use-organization-item.component.html | `npm run lint -- --quiet` (all files pass) & `npx jest --testPathPattern innovation-use` (230/230 passed) | [SPEC:quick/count-placeholder-copy] |
| 2026-09-02 | quick/org-type-description — show the CLARISA description next to the name (bold name, normal `: description`, name only when null) in the Organization type and Sub-type dropdown items of Innovation use details | client/research-indicators/src/app/pages/platform/pages/result/pages/innovation-use-details/components/innovation-use-organization-item/innovation-use-organization-item.component.html | `npx ng build --configuration development` (template compile pass, no new warnings) & `npm test -- --silent` (317 suites / 6793 tests passed). Scope limit (KZ-017): neither gate reaches the new markup — a `pTemplate="item"` renders only in an opened overlay panel and `let-` vars are untyped `any`, so the build cannot type-check the field names; `name`/`description` were confirmed against the wire payload of `/api/tools/clarisa/institutions-types/depth-level/1` and the `ClarisaInstitutionsSubTypes` interface. Visual check pending in the browser. | [SPEC:quick/org-type-description] |
| 2026-09-02 | quick/org-type-selected-description — mirror the name/description rendering into the closed control (`selectedItem` template) of the Organization type and Sub-type selects in Innovation use details, truncating to one line | client/research-indicators/src/app/pages/platform/pages/result/pages/innovation-use-details/components/innovation-use-organization-item/innovation-use-organization-item.component.html | `npx ng build --configuration development` (template compile pass, no new warnings) & `npm test -- --silent` (317 suites / 6793 tests passed). Placeholder preservation verified in the rendered-output producer, not inferred: primeng-select.mjs:2588 gates the `selectedItem` outlet on `&& selectedOption`, with `defaultPlaceholder` as the else-branch. Scope limit (KZ-017): as with quick/org-type-description, neither gate reaches the new markup — `let-` vars are untyped `any` (observed: a bogus property kept the build green), so field names rest on the CLARISA payload + `ClarisaInstitutionsSubTypes`. Visual check pending. | [SPEC:quick/org-type-selected-description] |
| 2026-09-03 | quick/innovation-use-add-button-style — align the three "Add other" buttons (actor / organization / measure) of Innovation use details with innovation-dev's treatment: add `rounded-[7px]` and move the label from `--ac-light-blue-400` to `--ac-light-blue-300` (#1689ca). Border was already `--ac-light-blue-300`. **Human-decided accepted WCAG exception:** #1689ca on `--ac-white-1` measures 3.84:1 (computed, not assumed), which clears 1.4.11 non-text (>= 3:1, the 2px border) but NOT 1.4.3 AA for the 15px label — this knowingly reverts DD-17's text token for this role only; the four prose links and the level stepper keep `--ac-light-blue-400`. No global class exists for this button: innovation-dev hardcodes hex (`border-[#1689CA]`), which the client guide forbids, so the token form was kept here. | client/research-indicators/src/app/pages/platform/pages/result/pages/innovation-use-details/innovation-use-details.component.html, innovation-use-details.component.spec.ts | `npx jest --testPathPattern innovation-use-details.component.spec` (148/148 passed) & `npx eslint` (0 errors). K-004: the rewritten pinning test was observed FAILING on purpose — reverting the HTML to `--ac-light-blue-400` reddened it (1 failed / 147 passed) with the received class string as evidence, then restored. Scope limits (KZ-017): eslint does not lint `*.spec.ts` ("File ignored because no matching configuration was supplied"), so the spec edit is covered by jest only; and the contrast assertion is arithmetic over tokens transcribed from colors.scss, not a measurement of the rendered pixel — dark theme (`--ac-light-blue-300` = #2b5986 there) is not asserted. `npx prettier --check` flags both files, but **it already flagged them on clean HEAD** (verified via `git stash`), so it is pre-existing and was deliberately not `--write`-fixed to keep the diff at 3 lines. | [SPEC:quick/innovation-use-add-button-style] |
| 2026-09-03 | quick/innovation-use-eyebrow-grey — set the ACTOR # / ORGANIZATION # card eyebrows of Innovation use details to #8d9299 (`--ac-grey-600`), matching innovation-dev's eyebrow colour. **MEASURE # needed no edit** — the shared `quantification-item` already hardcodes `text-[#8D9299]`, so it was left alone and OICR details (the other consumer of that shared component) is untouched. **Human-decided accepted WCAG exception, emphasized by the requester:** `--ac-grey-600` on the card's `--ac-grey-100` background measures 2.91:1 (computed, not assumed), failing 1.4.3 AA (>= 4.5:1) *and* the 3:1 large-text floor — at 13px this text is not large. Deeper than the sibling quick/innovation-use-add-button-style exception (3.84:1); both knowingly revert DD-17 for their own role only. No global class exists: innovation-dev hardcodes hex (`text-[#8D9299]`), which the client guide forbids, so the token form was used here. | client/research-indicators/src/app/pages/platform/pages/result/pages/innovation-use-details/components/innovation-use-actor-item/innovation-use-actor-item.component.html, .../innovation-use-organization-item/innovation-use-organization-item.component.html, innovation-use-details.component.spec.ts | `npx jest --testPathPattern innovation-use` (6 suites / 237 tests passed) & `npx eslint` (0 errors, no output). K-004: both rewritten pinning tests were observed FAILING on purpose — reverting the two templates to `--ac-grey-800` reddened exactly them (2 failed / 146 passed) with the asserting line as evidence, then restored. Scope limits (KZ-017): eslint does not lint `*.spec.ts`, so the spec edit rests on jest alone; the contrast figure is arithmetic over tokens transcribed from colors.scss, **not** a measurement of rendered pixels; and **dark theme is not asserted** (there `--ac-grey-600` = #949494 on `--ac-grey-100` = #2b2b2b, a different and unmeasured pair). Visual check pending in the browser. | [SPEC:quick/innovation-use-eyebrow-grey] |
| 2026-09-03 | quick/innovation-use-comment-chip-style — align every comment chip (callout) in Innovation use details with innovation-dev's treatment: left border and icon both `--ac-light-blue-500` (#074b86) and the icon vertically centered. Three chips changed: `use-level-guidance` and `evidence-callout` had **no icon at all**, so a `pi pi-exclamation-circle` was added to each (human-chosen from three options — glyph matched to innovation-dev rather than to innovation-use's own `info`); the ACTORS chip kept its existing `material-symbols` `info` glyph and only had `items-start` → `items-center` plus the recolor. The organization chip needed **no edit** — it already was `items-center` + `--ac-light-blue-500` + `pi-exclamation-circle`, i.e. the target pattern already existed in the codebase. `evidence-callout`'s two paragraphs gained a `flex flex-col` wrapper so the icon centers against the whole text block, not the first paragraph. **No WCAG exception here — this change IMPROVES contrast:** #074b86 on `--ac-grey-100` measures 8.27:1 vs the previous #1689ca at 3.57:1 (both computed). | client/research-indicators/src/app/pages/platform/pages/result/pages/innovation-use-details/innovation-use-details.component.html | `npx jest --testPathPattern innovation-use` (6 suites / 237 tests passed) & `npx ng build --configuration development` (template compiles; `innovation-use-details-component` chunk 96.66 kB, zero warnings attributed to it — the two NG8112 warnings are pre-existing in PoolFundingAlignmentComponent/CreateOicrFormComponent) & `npx eslint` (0 errors). The jest run is real evidence for exactly one property: the new wrapper `div` did **not** break the `[data-testid="evidence-callout"] p` / `use-level-guidance li` descendant queries. Scope limits (KZ-017): **no automated gate in this repo asserts a left-border colour, an icon colour, or vertical alignment**, so the actual visual outcome of this change is verified by the build's template validity + a human browser check only — not by a test. Dark theme unasserted (there `--ac-light-blue-500` = #1b3450). | [SPEC:quick/innovation-use-comment-chip-style] |
| 2026-09-03 | quick/innovation-use-level-fill — set the SELECTED level circle's background in the Innovation use level stepper to #1689ca (`--ac-light-blue-300`), from `--ac-light-blue-400` (#035ba9). One class in the ternary; the circle's border was already `--ac-light-blue-300` and the unselected digits keep `text-[var(--ac-light-blue-400)]` on white. **Human-decided accepted WCAG exception — the THIRD in this template's family:** white-on-#1689ca measures 3.84:1 (computed), clearing 1.4.11 non-text (>= 3:1) but not 1.4.3 AA for the digit, which is `fs-[16]` at normal weight and therefore not large text. This directly reverses R3 remediation **F-1** (validation-report.md), which had re-derived this exact fill away from #1689ca for this exact reason. Siblings: quick/innovation-use-add-button-style (3.84:1) and quick/innovation-use-eyebrow-grey (2.91:1). | client/research-indicators/src/app/pages/platform/pages/result/pages/innovation-use-details/components/innovation-use-level-stepper/innovation-use-level-stepper.component.html, innovation-use-level-stepper.component.spec.ts, innovation-use-details.component.spec.ts | `npx jest --testPathPattern innovation-use` (6 suites / 237 tests passed) & `npx eslint` (0 errors). K-004: observed FAILING on purpose — reverting the fill to `--ac-light-blue-400` reddened exactly 2 tests (2 failed / 235 passed): the rewritten pinning test and the stepper's own c3 highlight assertion; then restored. A latent trap was fixed while rewriting: the unselected-digits test filtered by the ABSENCE of `bg-[var(--ac-light-blue-400)]`, so after this change it would have reclassified the selected button as unselected and asserted the wrong text token against it — the filter now keys positively on `bg-[var(--ac-white-1)]`, the class the unselected branch actually sets. Scope limits (KZ-017): eslint does not lint `*.spec.ts`; the ratio is arithmetic over colors.scss tokens, **not** a rendered-pixel measurement; dark theme unasserted (there `--ac-light-blue-300` = #2b5986 and `--ac-white-1` differs, an unmeasured pair). | [SPEC:quick/innovation-use-level-fill] |
| 2026-09-03 | quick/innovation-use-org-selected-html — render the CLARISA institution label as HTML in the CLOSED Organization select of Innovation use details. The select had a `pTemplate="item"` with `[innerHTML]`, but **no `selectedItem` template**, so the collapsed control fell back to `[optionLabel]="'html_full_name'"` interpolated as plain text and displayed the literal markup (`<strong>WUR</strong> - Wageningen University...`). Added a `selectedItem` template with `[innerHTML]="institution.html_full_name"` and `min-w-0 truncate`, matching the `selectedItem` shape already used by the Organization type / Sub-type selects in the same file. No token, colour, or logic change. Not copied from innovation-dev: that component's `selectedItem` renders the literal string "Organization selected", which loses the institution name — the acronym is kept bold here instead. | client/research-indicators/src/app/pages/platform/pages/result/pages/innovation-use-details/components/innovation-use-organization-item/innovation-use-organization-item.component.html | `npm test -- --silent` (317 suites / 6793 tests passed — includes the organization-item spec, which renders this template, so it is real evidence the template compiles) & `npx eslint` (exit 0, no output). Placeholder preservation verified in the rendered-output producer, not inferred: primeng-select.mjs:2588 gates the `selectedItem` outlet on `&& selectedOption`, with `defaultPlaceholder` as the else-branch (same check as quick/org-type-selected-description). Scope limits (KZ-017): **no test in this repo asserts the closed control's rendered HTML**, so the actual fix — bold acronym instead of literal `<strong>` — rests on the PrimeNG source above plus a human browser check, not on a gate; eslint on a `.html` file checks template syntax, not rendered output. Angular's default sanitizer permits `<strong>`; `html_full_name` is CLARISA-sourced, the same value already trusted by the open-panel `item` template. | [SPEC:quick/innovation-use-org-selected-html] |
| 2026-09-04 | quick/innovation-use-level-title-bold — make the selected level's composed title (`{{ level }} - {{ name }}`) render bold in the Innovation use level stepper, so it is visually distinct from the definition below it. The template already carried `font-bold`, but it was **inert**: Tailwind here is the v4 **browser runtime** (`<script src="…@tailwindcss/browser@4.1.6…">` in `src/index.html`, no build-time Tailwind and no PostCSS in this repo), which declares `@layer theme, base, components, utilities` — and **unlayered CSS outranks any layered utility regardless of specificity**, so the unlayered `.option-label { font-weight: 400 }` in `src/styles/custom-fields.scss` silently won. Changed `font-bold` → `!font-bold` (compiles to `font-weight: 700 !important`, which does beat an unlayered non-important declaration). This is the same reason the surrounding template already uses `!w-8`, `!flex`, `!rounded-full`. No token, colour, contrast, or logic change; the `.description` paragraph is untouched and stays weight 400. | client/research-indicators/src/app/pages/platform/pages/result/pages/innovation-use-level-stepper/innovation-use-level-stepper.component.html (path: .../innovation-use-details/components/innovation-use-level-stepper/) | `npm test -- --silent` (317 suites / 6798 tests passed — includes the stepper's own spec, which renders this template, so it is real evidence the template still compiles) & `npx eslint <component dir>` (exit 0, no output). The cascade-layer mechanism was **verified, not assumed** (K-004): the CDN bundle the app actually loads was downloaded and `@layer theme, base, components, utilities` found in it, and `grep -rn "@layer" src/styles/*.scss` returned nothing, confirming `.option-label` is unlayered. Scope limits (KZ-017): **no test in this repo asserts a computed font-weight or the rendered pixel** — jsdom does not run the Tailwind browser runtime at all, so the suite cannot see this class either way; eslint on a `.html` file checks template syntax, not rendered output. The visual outcome rests on the layer evidence above plus a human browser check. Dark theme is unaffected (font-weight carries no theme token). | [SPEC:quick/innovation-use-level-title-bold] |
| 2026-09-08 | `quick/evidence-next-innovation-use` — add indicator 6 to Evidence's `showNext` allowlist so the Next button appears and navigates to IP rights. The navigation was already unconditional (`evidence.component.ts` → `navigateTo('ip-rights')`), and IP rights' Back already returned to Evidence for every indicator — only the button was hidden, so the round trip was broken in one direction | `client/.../result/pages/evidence/evidence.component.html` (1 line) | `evidence.component.spec` 48/48 · `npx eslint` exit 0 · `npm run build` exit 0. **`showNext` has no automated owner** — the button's appearance is browser-only evidence | `[SPEC:quick/evidence-next-innovation-use]` |
| 2026-09-08 | `quick/innovation-use-evidence-callout-gating` — gate the Innovation use details evidence callout (P1 "Please provide a brief explanation justifying the selected Innovation Use Level…" + P2 documentation list) on `showJustification()`, the existing level ≥ 6 rule, so it appears with the Justification field instead of at every level. **This REVERSES an approved acceptance criterion, by explicit user ruling after the escalation was raised and declined**: archived `R-IUP-021` **AC.5** ("The block renders at **every** level state (none, `0`, `9`) … it is **not** attached to the conditional justification textarea") and its scenario "Evidence guidance is not gated on the justification's condition". The spec's stated reason for AC.5 was that the copy would vanish where reporters least expect the evidence requirement; the counter-reason accepted here is that P1 asks for a justification field that does not exist below level 6 (`R-IUP-006`), so the callout requested something the form did not offer. **Whole callout gated, not P1 only** — user ruling. `showJustification()`, `justificationMissing()` and `justificationWhitespaceOnly()` are untouched (the AC.5 scenario's second clause survives), and the callout is a SEPARATE `@if` from the textarea's block so it keeps its position above the textarea. Spec `c6` was rewritten rather than deleted: its guidance/definitions-link half is unchanged (`R-IUP-020` AC.5 still holds and is now asserted at 6 levels), and its purpose-built falsifying test — written to catch exactly this `@if` wrap — was **inverted**, not removed, plus new level-5/level-6 boundary and DOM-order tests. | client/research-indicators/src/app/pages/platform/pages/result/pages/innovation-use-details/innovation-use-details.component.html · …/innovation-use-details.component.spec.ts | **Gate proven able to fail first (K-004):** reverting the template to `@if (true)` reddened exactly 5 tests, all in `c6`, none elsewhere — then restored and re-measured. `npm test -- --silent` (317 suites / **6889** tests passed, full suite, quiet tree) · `npx ng build --configuration development` (template type-checks under `strictTemplates`; the two NG8112 warnings are pre-existing in PoolFundingAlignmentComponent/CreateOicrFormComponent) · `npx tsc -p tsconfig.spec.json --noEmit` → **0 errors in the touched spec file**, tripwire total 934 (a real diagnostic run, not a K-004 parse-abort) · `npx eslint` exit 0 · zero hex literals. **Scope limits (KZ-017):** no gate here proves the *visual* result — the level-5 screenshot that prompted this was human-observed, and the fix is confirmed in jsdom (element absent/present), not in a painted browser. Dark theme unasserted. The archived `requirements.md` was **not** edited — it is a point-in-time record; this row is the only place the AC.5 reversal is written down, so a future reader of `R-IUP-021` will find AC.5 contradicted by the code and must land here. | [SPEC:quick/innovation-use-evidence-callout-gating] |
| 2026-09-08 | `quick/innovation-use-level-connector-gap` — remove the `mt-3` from the selected level's definition box in the Innovation use level stepper, so the vertical connector below the selected bubble (`w-0.5 h-10 bg-[var(--ac-light-blue-200)]`) physically meets the box's top border instead of stopping 12px short. User-diagnosed (`por lo que vi es un margin`) and confirmed: `mt-3` = 0.75rem = 12px, and the connector's bottom already coincides with the stepper row's bottom edge (the button+connector column is the row's tallest child), so that margin was the entire gap. **One class deleted; no token added, no color/size introduced, no other spacing touched** — the box's own `p-5`, the row's `mt-3` and the connector's `h-10` are unchanged. | client/research-indicators/src/app/pages/platform/pages/result/pages/innovation-use-details/components/innovation-use-level-stepper/innovation-use-level-stepper.component.html | `npm test -- --silent` (317 suites / 6889 tests passed, full suite, quiet tree) · `npx ng build --configuration development` (bundle generation complete, template valid under `strictTemplates`) · `npx eslint` exit 0 · zero hex literals. **Scope limits (KZ-017): none of these gates asserts a margin, a gap, or any painted geometry — a grep of the stepper spec for `mt-3`/`h-10`/`w-0.5` returns NOTHING, so no test covered this class before the edit and none covers its absence after.** The suite is therefore evidence for exactly one property: deleting the class broke no existing assertion. The actual visual outcome (connector touching the border) is verified by the human screenshot review only. Dark theme unasserted. Two adjacent observations left UNFIXED and out of scope: the stepper row is `items-center`, so the selected column being 40px taller than its siblings pulls the grey `h-[2.5px]` segments below the bubbles' centre line; and this component uses raw Tailwind spacing (`mt-3`, `p-5`, `h-10`) rather than the repo's `rs-*` tokens. | [SPEC:quick/innovation-use-level-connector-gap] |
| 2026-09-08 | `quick/innovation-use-guidance-spacing-and-link-colour` — two edits to the Innovation use details guidance banner. **(1)** Add `rs-mt-[8]` to the `use-level-guidance` callout: `span.label` above it is inline and `custom-fields.scss` sets only font + colour on `.label` (no margin), so the banner sat flush against "How would you assess the current use level of the innovation?". The gap is opened at the CALL SITE, deliberately not on the shared `.label` rule, which is used across the app. **(2)** Recolour the calculator link (`TRY THE NEW INNOVATION USE CALCULATOR`) from `--ac-light-blue-400` to **`--ac-grey-700`** — the user supplied the raw value and `--ac-grey-700` in `colors.scss` already IS that value, so **no token was added and no hex is spelled in the component** (DD-7). ⚠️ **This is a DELIBERATE, MEASURED WCAG exception, per the standing user ruling that visual consistency outranks AA on cosmetic colour calls:** `--ac-grey-700` on `--ac-grey-100` computes **3.91:1**, below the 4.5:1 that **R-IUP-020 AC.6** and **NFR-IUP-001** require; the previous `--ac-light-blue-400` measured 6.35:1. It clears the 3:1 large-text/non-text floor only. The exception is **contained to this one link** — the sibling definitions link and the evidence button keep `--ac-light-blue-400` and remain AA, and a test now asserts that separation in both directions so the grey cannot spread silently. | client/research-indicators/src/app/pages/platform/pages/result/pages/innovation-use-details/innovation-use-details.component.html · …/innovation-use-details.component.spec.ts | **Both gates proven able to fail BEFORE being cited (K-004), and the probe changed the work:** reverting the link colour reddened exactly 1 test, but reverting the margin reddened **ZERO** — nothing in the suite covered `rs-mt-[8]`, so that half could have been dropped by any later edit in silence. A class-presence guard was added for it and re-probed (1 red, restored, green). `c12`'s "all three links carry `--ac-light-blue-400`" was SPLIT into two tests rather than weakened, and the 3.91:1 shortfall is now **asserted as a number** so the deviation lives in the suite instead of being undocumented. `npm test -- --silent` (317 suites / **6892** tests passed, full suite, quiet tree) · `npx ng build --configuration development` (bundle complete, `strictTemplates` valid) · `npx tsc -p tsconfig.spec.json --noEmit` → 0 errors in the touched spec · `npx eslint` exit 0 · `grep -cE '#[0-9a-fA-F]{3,8}'` on the component → **0**. **Scope limits (KZ-017): jsdom paints nothing — the new guard asserts the CLASS `rs-mt-[8]` is present, never that 8px renders, and no gate here checks the link's actual painted colour either.** Both visual outcomes are human-verified only. Dark theme unasserted: `--ac-grey-700` is redefined there and its dark-mode ratio was NOT computed. | [SPEC:quick/innovation-use-guidance-spacing-and-link-colour] |
| 2026-09-08 | `quick/innovation-use-banner-body-grey-700` — **corrects the previous row's second half.** `quick/innovation-use-guidance-spacing-and-link-colour` recoloured the *calculator link*; the user's intent was the opposite — links stay blue, and the **non-link body text of EVERY information banner** moves to `--ac-grey-700`. Calculator link reverted to `--ac-light-blue-400` (6.35:1, AA). A repo-wide grep for the banner pattern (`border-l-[var(--ac-light-blue-500)]`) found **exactly 4 banners, all inside this feature** — no app-wide sweep was needed and none was performed: `use-level-guidance` (4 `<li>`), `evidence-callout` (2 `<p>`), the ACTORS callout (2 `<span>`) and the organization card's known-institution callout (1 `<span>`). ⚠️ **DELIBERATE, MEASURED WCAG exception, superseding DD-17's `--ac-grey-800` body choice, per the standing user ruling that visual consistency outranks AA on cosmetic colour calls.** Ratios computed, not eyeballed: `--ac-grey-700` is **3.91:1** on `--ac-grey-100` (three banners) and **3.51:1** on `--ac-grey-200` (the organization banner — the WORST of the four, and a different number precisely because the background differs); `--ac-grey-800` measured 7.44:1 and 6.68:1. Both clear the 3:1 non-text floor only, and both fail the 4.5:1 that **R-IUP-020 AC.6 / R-IUW-002 / NFR-IUP-001** require. **Two surfaces deliberately NOT touched and still AA:** the `loadFailed` **error** banner (red border/icon — not an information banner) and the definitions-link paragraph (sits on the white card, not inside a banner). No token added; `--ac-grey-700` already held the requested value, and no hex is spelled in either component. | client/research-indicators/src/app/pages/platform/pages/result/pages/innovation-use-details/innovation-use-details.component.html · …/components/innovation-use-organization-item/innovation-use-organization-item.component.html · …/innovation-use-details.component.spec.ts | **Gates proven able to fail BEFORE being cited (K-004), twice:** reverting all banner bodies to `--ac-grey-800` reddened exactly **4** tests, one per banner; and re-applying the *previous* row's mistake (spreading the body grey onto the calculator link) reddened the link guard — so the correction is now regression-protected against repeating itself. Five assertions were **rewritten, none deleted**: the four class-presence checks flipped token, and each now asserts its own AA shortfall **as a number** alongside the superseded ratio, so the size of what was traded away is visible at the point of the trade. `npm test -- --silent` (317 suites / **6891** tests passed, full suite, quiet tree) · `npx ng build --configuration development` (bundle complete, `strictTemplates` valid) · `npx tsc -p tsconfig.spec.json --noEmit` → 0 errors in the touched spec · `npx eslint` exit 0 · hex literals in both components → **0**. **Scope limits (KZ-017): jsdom paints nothing — every check above asserts a CLASS NAME or computes arithmetic; none observes a rendered colour.** The visual outcome is human-verified only. **Dark theme is a REAL open risk here, not a formality:** `--ac-grey-100`/`--ac-grey-200` are redefined dark (#2b2b2b/#3e3e3e) and no dark-mode ratio for `--ac-grey-700` was computed in either theme direction. | [SPEC:quick/innovation-use-banner-body-grey-700] |
| 2026-09-08 | `quick/innovation-use-banner-leading-and-border` — reviewer-requested shape alignment of all **four** Innovation use information banners to the reference banner reviewers screenshotted, `innovation-details.component.html:106` ("At the output level, a single readiness score…"): **`border-l-[5px]`** (was `border-l-[4px]` on three banners and Tailwind's `border-l-4` shorthand on the organization one) and **`leading-[17px]`** on every banner body text element (4 `<li>` + 2 `<p>` + 2 `<span>` + 1 `<span>` = 9). **The reference was read, not copied:** it spells raw hex for background, border and text because it predates the token rule, so the TOKEN equivalents were used here — and comparing them showed background (`--ac-grey-100`), border colour (`--ac-light-blue-500`) and body colour (`--ac-grey-700`, from the previous row) **already matched**, leaving only width and line-height genuinely different. Two reference properties deliberately NOT copied, with reasons: `font-['Barlow']` is a **no-op** — `styles.scss:8` sets Barlow globally, so our banners already render it; and `my-[15px]` was not named by the reviewers and would fight our `rs-p-*` padding. | client/research-indicators/src/app/pages/platform/pages/result/pages/innovation-use-details/innovation-use-details.component.html · …/components/innovation-use-organization-item/innovation-use-organization-item.component.html · …/innovation-use-details.component.spec.ts | **Coverage gap found and closed BEFORE citing any gate (K-004):** as with `rs-mt-[8]`, **nothing in the suite asserted either property**, so both could have been dropped in silence — three guards were added across the two describes so all four banners are pinned, then each was probed: reverting `border-l-[5px]`→`[4px]` reddened **2**, dropping `leading-[17px]` reddened **3**. A first attempt at the ACTORS/organization guard **failed for the right reason and was fixed, not loosened**: selecting the banner by text alone matched an inner layout wrapper (`flex flex-col rs-gap-[4]`), so both selectors now require the border class AND the identifying copy — matching on the border class alone would not prove which banner was inspected. `npm test -- --silent` (317 suites / **6894** tests passed, full suite, quiet tree) · `npx ng build --configuration development` (bundle complete) · `npx tsc -p tsconfig.spec.json --noEmit` → 0 errors in the touched spec · `npx eslint` exit 0 · hex literals → **0** (one crept in through a COMMENT of mine quoting the reference's hex values and was removed — the DD-7 grep does not read intent). **Scope limits (KZ-017): every new guard is CLASS-PRESENCE ONLY.** jsdom paints nothing, so no check here observes a 5px border or a 17px line box; `border-l-[5px]`/`leading-[17px]` resolve in Tailwind, outside the test's reach. Whether the banners now actually match the reference is human-verified only. Dark theme unasserted. | [SPEC:quick/innovation-use-banner-leading-and-border] |
| 2026-09-08 | `quick/innovation-use-guidance-margin-symmetry` — raise the `use-level-guidance` callout's top margin from `rs-mt-[8]` to **`rs-mt-[16]`**, matching its existing `rs-mb-[16]`. Follow-up to `quick/innovation-use-guidance-spacing-and-link-colour`, which opened that gap at 8px: the user's DevTools box model showed margin `8` top / `16` bottom, so the callout was **asymmetric**, not merely tight. One class value changed; padding, border, colours and the bottom margin are untouched. | client/research-indicators/src/app/pages/platform/pages/result/pages/innovation-use-details/innovation-use-details.component.html · …/innovation-use-details.component.spec.ts | **Gate proven able to fail (K-004):** reverting to `rs-mt-[8]` reddens exactly 1 test. The existing guard was **strengthened rather than merely retargeted** — it now asserts `rs-mt-[16]` AND `rs-mb-[16]` together, because the defect being fixed is *top ≠ bottom*, which a one-sided check cannot see; the old guard would have passed on any non-zero top margin. `npm test -- --silent` (317 suites / **6894** tests passed, full suite, quiet tree) · `npx ng build --configuration development` (bundle complete) · `npx tsc -p tsconfig.spec.json --noEmit` → 0 errors in the touched spec · `npx eslint` exit 0 · hex literals → 0. **Scope limits (KZ-017): class-presence only — jsdom paints nothing, so this asserts the two classes are present, never that 16px renders on either side.** `.rs-mt-[16]`/`.rs-mb-[16]` resolve in responsive-size.scss, outside the test's reach; the visual symmetry is human-verified. | [SPEC:quick/innovation-use-guidance-margin-symmetry] |
| 2026-09-10 | `quick/innovation-dev-clear-icon` — the card's clear control was a 14px text glyph `⊗`; now the same PrimeIcons `pi-times-circle` at **18px** the approved OICR card uses, coloured with `--ac-red-1` (which *is* `#cf0808`, the hex that card hardcodes — and unlike it, ours adapts to `#ff4d4d` in dark) | `innovation-use-details.component.html` | suite 317/6963 · `npm run build` exit 0 · lint clean · hex count still 0 | `[SPEC:quick/innovation-dev-clear-icon]` |
| 2026-09-11 | `quick/innovation-dev-card-reorder` — reorder the Innovation Dev card to **title → metadata row → description → right-aligned anchor**; drop the `Innovation detail →` label so the anchor stands alone. ⚠️ **Outgrew the triviality gate:** it invalidated 7 assertions across 5 existing tests (separator counts, a nesting chain, a child count), which needed reasoning rather than a text swap — finished as a quick because none of it is *new* coverage, only existing assertions retargeted at the new structure | `innovation-use-details.component.{html,spec.ts}` | suite **327/7388** green (incl. other work merged into `dev`) · `npm run build` exit 0 · hex still 0 | `[SPEC:quick/innovation-dev-card-reorder]` |
| 2026-09-11 | `quick/innovation-measures-remove-comments` — remove the **Comments** field from the Innovation Use **OTHER QUANTITATIVE MEASURES** cards. Front-end only, per explicit user ruling that this must NOT go through `/akili-propose`: the field is optional on the payload (`description?: string`), so the page simply stops sending it — no endpoint, DTO, entity or migration is touched, and the server contract is unchanged. **`QuantificationItemComponent` is SHARED with OICR details (2 call sites there), so the field was NOT deleted from the card** — a new `@Input() showComments = true` guards the label+`app-textarea` block, and only Innovation Use passes `false`. OICR renders byte-identically (its 2 suites re-run green). Payload: `description` dropped from `buildPayload()`'s quantification map, so it is OMITTED from the JSON rather than sent as `null`. **One knock-on fixed, not left latent:** `quantificationRowAbsent()` also keyed on `description`, so a legacy row carrying ONLY a comment would have survived the blank-row filter and been POSTed as `{id, unit: ''}` with no number — `description` was removed from that predicate too. ⚠️ **This is not purely cosmetic** (a field leaves the payload) and would normally escalate per the triviality gate; finished as a quick by explicit user ruling (*"no hay tiempo para ajustes con propouse"*), recorded here rather than waived silently. | `quantification-item.component.{ts,html}` · `innovation-use-details.component.{html,ts,spec.ts}` | **Gates proven able to fail BEFORE being cited (K-004), and two of them reddened on their own:** the edit broke 2 pre-existing tests unprompted — the `c10` required-messaging test (Comments label present) and `T-08 c3` ("keeps a row identified only by unit **or only by description**") — which is direct evidence both behaviours were genuinely covered before the change; both were **retargeted at the new behaviour, not deleted** (the first now asserts the label is absent AND no `app-textarea` renders on the card; the second asserts the unit-only row survives and the description-only row is dropped). A THIRD, new test ("never sends description, even when the row carries one from the server") was probed by re-adding `description: row.description` to `buildPayload()` → exactly 1 red, then restored. `npm test -- --silent` → **327 suites / 7389 tests passed** (full suite) · `npx tsc -p tsconfig.app.json --noEmit` exit 0 · `npm run lint -- --quiet` → "All files pass linting" · no hex literal added. **Scope limits (KZ-017):** jsdom paints nothing — the render check asserts the element is ABSENT from the DOM, never that the card's layout reflows correctly without it; the surviving `[commentsRequired]="false"` binding at the call site is now dead but harmless and was deliberately left untouched. No server-side test ran: the claim that omitting `description` is accepted is read off the payload type being optional, NOT off a live POST — unverified against the running API. Dark theme unasserted. | `[SPEC:quick/innovation-measures-remove-comments]` |
| 2026-09-11 | `quick/innovation-measures-unit-first` — swap the field order on the Innovation Use **OTHER QUANTITATIVE MEASURES** cards to **Unit → Number** (was Number → Unit). **Same shared-card constraint as the row above:** `QuantificationItemComponent` is also OICR details' card, so the order is a new `@Input() unitFirst = false` (today's order stays the default, OICR untouched) and only Innovation Use passes `true`. **Reordered in the DOM, deliberately NOT with a CSS `order` swap:** the two fields moved into `ng-template` + `ngTemplateOutlet` so the markup exists once and the outlet order carries the decision — a `order-1`/`order-2` class swap would have left tab order and screen-reader order in the OLD sequence while only the paint moved, which is the classic reading-order a11y defect. Costs one `NgTemplateOutlet` import. | `quantification-item.component.{ts,html}` · `innovation-use-details.component.{html,spec.ts}` | **The swap broke 4 pre-existing tests on its own, and the breakage was the point:** every one of them located the Number field as *"the first `app-input` in the card"* (`quantCard.query(By.directive(InputComponent))`) — a positional selector that silently retargets when the order changes, which is exactly how a reorder corrupts a suite without reddening it. All 6 such lookups (4 red + 2 that happened to still pass) were replaced with one `numberFieldOf()` helper that selects by `optionValue === 'number'`, order-independent and non-null-asserted so a vanished field fails loudly instead of reading whichever input is first. **New guard probed at red before being cited (K-004):** flipping `[unitFirst]` back to `false` reddens exactly 1 test. The guard asserts **DOM order**, not a class or an `order` property — DOM order is what tab/AT order follows and the only one of the three jsdom can observe — and it checks the **labels move with the inputs**, since a card that swapped inputs but left labels in place would pass an inputs-only assertion and be wrong on screen. `npm test -- --silent` → **317 suites / 6965 tests passed** (full suite, on `AC-1679-Create-the-innovation-use-section`; the 327/7389 figure in the row above was measured on `dev`, which carries more merged work — not a regression) · `npx tsc -p tsconfig.app.json --noEmit` exit 0 · `npm run lint -- --quiet` → "All files pass linting" · OICR's 2 call sites re-run green on the default `false`. **Scope limits (KZ-017):** jsdom paints nothing — nothing here proves the two fields *look* right stacked or side-by-side in the `xl:grid-cols-2` breakout, and the grid's column assignment at ≥xl is unverified; the visual result is human-verified only. Tab order is asserted only by proxy (DOM order), never by driving focus. Dark theme unasserted. | `[SPEC:quick/innovation-measures-unit-first]` |
| 2026-09-11 | `quick/innovation-use-sr-only-containing-block` — fix the user-reported **double scroll** on Innovation Use details: scrolling anywhere (notably over the fixed left menu) moved the section container independently of the app's scrollport, leaving content clipped. **Root cause, measured in the live page rather than reasoned from CSS:** the `<span class="sr-only">(opens in a new tab)</span>` inside the *View innovation detail* anchor. Tailwind's `.sr-only` is `position: absolute`, and that span had **no positioned ancestor**, so its containing block resolved all the way up to `.grid-container` (`position: relative`) — which sits **outside** `#content`, the element that actually owns the app's `overflow-y: auto`. The 1px span therefore escaped the scrollport entirely and extended `.grid-container`'s scrollable overflow to its own static position (~2299px), and since `.grid-container` is `overflow: visible` that leaked straight into `body` and `<html>`. Fix is **one word**: `relative` on the anchor, which makes the anchor the containing block so `#content` clips the span like everything else. This also explains the two facts that broke every earlier hypothesis: **why only this page** (it holds the only `sr-only` of the 13 wizard pages) and **why the overflow number drifted** 160→176 between measurements (the span's static position moves with the inner scroll, so the leak is a function of scroll position). | `innovation-use-details.component.{html,spec.ts}` | **Gate proven able to fail BEFORE being cited (K-004):** removing `relative` reddens exactly 1 test; restored and re-measured. `npm test -- --silent` → **317 suites / 6965 tests passed** · `npx ng build --configuration development` exit 0. The guard was added to an EXISTING anchor test rather than as a new one, because this exact block was restructured earlier the same day by `quick/innovation-dev-card-reorder` — an unguarded `relative` would be lost in the next edit with nothing reddening. **Two earlier hypotheses were WRONG and are recorded here rather than quietly dropped**, since both were reasoned from the cascade instead of measured: (1) `overflow-x: hidden` computing `overflow-y: auto` on `.app-page-wrapper` / the routed page host — implemented as `overflow-x: clip`, did not fix it, **fully reverted**; the live numbers later showed `.app-page-wrapper` sits inside `#content`, which clips correctly (`clientH 961` / `scrollH 4073`), so it never could have been the cause. (2) a horizontal overflow forcing a scrollbar — killed by `scrollWidth - clientWidth === 0`. **Scope limits (KZ-017):** jsdom resolves no containing block and paints nothing, so the test asserts only that the CLASS is present — the causal chain (containing block → scrollport → document height) is verified **only** by the in-browser measurements above, not by any suite. The fix is confirmed working by human visual check. **Not fixed, deliberately:** 6 other `.sr-only` occurrences exist (navbar, multiselect, project-detail, submit-result, project-results-table, result-ai-assistant) carrying the identical latent pattern — left alone because those pages are reported working and no measurement was taken there. A durable fix would stop `.sr-only` depending on someone remembering `relative` on its parent; that is a shared-utility change and belongs in a spec, not a quick. | `[SPEC:quick/innovation-use-sr-only-containing-block]` |
| 2026-09-11 | `quick/innovation-use-actors-total-field` — QA on the ACTORS card: *"The total number of actors looks odd. Maybe it's best to leave it in a non-editable text field, right-aligned, to keep the look of the section."* The bare bold figure becomes a **disabled `pInputText`** placed in the RIGHT column of the same `grid-cols-1 lg:grid-cols-2` the four count fields use, so it lines up under `Women non-youth` / `Men non-youth`, with a real `<label for>` above it. **`app-input` was deliberately NOT reused**: its `number` branch hardcodes `[showButtons]="true"`, so it would render spinner arrows on a field that cannot be edited. **The mechanism is copied from the app, not invented** — `general-information`'s `Title` passes plain `[disabled]` and gets the bluish fill the QA screenshot shows; that fill is the theme's `formField.disabledBackground` (`{surface.200}` = `#e2e8f0`), so no hex and no new token enter the component. **Two corrections made mid-task, both worth recording because each reversed a decision already argued for in this session:** (1) the field was first built `readonly` on the stated ground that `disabled` would dim the digits via the theme's `disabledColor` — **that reasoning was wrong for this app**: `custom-fields.scss` forces `color: #4c5158 !important` on every input in the page wrapper and outranks the theme token, which is why the reference `Title` field renders full-strength text; (2) a hand-rolled `!bg-[var(--ac-grey-200)]` was added to fake the greyed look and then **deleted** once `disabled` supplied the app's real one. **A user-spotted defect closed on the way:** `readonly` inputs are still `:enabled`, and PrimeNG paints both its hover border and its blue focus border through `.p-inputtext:enabled:hover` / `:enabled:focus` (`primeng-inputtext.mjs:29,33` — read in node_modules, not assumed), so the readonly version kept inviting an edit it could not accept; `disabled` matches neither selector, so the affordance disappears with no extra CSS. **MEASURED contrast:** `#4c5158` on `#e2e8f0` = **6.49:1**, above the 4.5:1 floor (it was 7.44:1 as a span on the card's `--ac-grey-100`). | `innovation-use-actor-item.component.{html,spec.ts}` · `innovation-use-details.component.spec.ts` | **Gate proven able to fail BEFORE being cited (K-004):** dropping `disabled` reddens exactly **3** tests — `c6`, the contrast test, and `c13 — read-only when not editable`, the last of which already existed and asserts every input in the cards is disabled, so the new field fell under prior coverage without being asked to. `npm test -- --silent` → **317 suites / 6965 tests passed** · `npx ng build --configuration development` exit 0 · `npm run lint -- --quiet` → "All files pass linting" · no hex added to any component. **10 tests broke on the first edit; NONE was deleted.** Seven only read the figure and moved from `textContent` to `.value`. One — the labels test — was fixed by **widening** the selector plus an `app-input` filter, because narrowing it to `span.label` would have kept it green while silently no longer covering the Total label at all. And **`c6` was INVERTED by explicit user ruling**: its criterion (*"total is read-only text, not an input"*) was written to forbid the exact shape QA later requested, so the SHAPE assertion is reversed — but c6's stated GUARANTEE (*"the total control cannot receive a typed value"*) is kept and now asserted more strictly than a span ever could: `disabled` + no `p-inputNumber` + no `ngModel` write-back. **Scope limits (KZ-017):** jsdom paints nothing — the contrast figure is arithmetic over two known hexes, and nothing here observes the rendered fill, the right-alignment, or the grid placement; all three are human-verified only. **Dark theme is a REAL open risk, not a formality:** `custom-fields.scss`'s `color: #4c5158` has no dark variant, so in dark mode this value drops to ~1.3:1 on the disabled fill — but the four sibling count fields are already ~1.8:1 for the same reason, so this change makes the Total consistent with an app-wide pre-existing defect rather than introducing a new one. The span it replaced used `--ac-grey-800`, which DOES flip dark (7.95:1); that specific advantage is lost. | `[SPEC:quick/innovation-use-actors-total-field]` |
| 2026-09-11 | `quick/innovation-use-bottom-clearance` — give the last card on Innovation Use details room to breathe above the fixed Back/Next/Save bar. **The request's premise did not hold and the fix is at a different level than asked:** the measures card was reported as missing the bottom margin the other cards have, but it already carries `rs-mb-[25]`, byte-identical to all four siblings (lines 12/156/291/340/368). The crowding is a CLEARANCE deficit that belongs to the page, not to the card — `.app-page-wrapper` reserves `margin-bottom: 70px` (`styles.scss`) while `.result-nav-buttons-fixed` measures **99px** (read off the live page in this session's console table, not estimated), so the fixed bar covers the whole reserve plus 29px more and swallows the card's 25px entirely, leaving ~0 visible. Raising the reserve on THIS page's wrapper to `!mb-[100px]` yields `100 + 25 - 99 = ~26px` actually visible, matching the inter-card gap. **Adding margin to the card as literally requested could not have worked**: `rs-mb-*` is generated only for 1-30px (`responsive-size.scss`), so the reachable ceiling is +5px, and it would still land under the bar. `!` is required because the class overrides `.app-page-wrapper`'s own rule at equal specificity. | `innovation-use-details.component.{html,spec.ts}` | **Gate proven able to fail BEFORE being cited (K-004):** removing the class reddens exactly 1 test. `npm test -- --silent` → **317 suites / 6966 tests passed** · `npx ng build --configuration development` exit 0 · `npm run lint -- --quiet` → "All files pass linting". **A coverage gap was found and closed rather than noted:** NOTHING in the suite asserted the page wrapper's spacing, so the class could have been dropped by any later edit to this template in complete silence — the same failure mode that already hit `rs-mt-[8]` in this log on 2026-09-08. **Scope limits (KZ-017):** the guard is CLASS-PRESENCE ONLY — jsdom computes no layout, so nothing here observes 26px of gap, or the bar's height, or any overlap; the arithmetic above rests on one browser measurement taken at one viewport, and the visual result is human-verified only. Dark theme irrelevant (spacing only). **Not fixed, deliberately:** the 29px deficit lives in `.app-page-wrapper`, which all **13** wizard pages share — every one of them crowds its last card the same way, and this row fixes exactly one. A root fix (raising the global reserve to ~130px) shifts the bottom spacing of every result page at once and belongs in a spec, not a quick. | `[SPEC:quick/innovation-use-bottom-clearance]` |
| 2026-08-23 | quick/pool-funding-post-save-wiring — **Triviality Gate FAILED (behavior change, bugfix); fast-tracked on explicit user authorization instead of `/akili-propose`.** Two bugs on the Pool Funding Alignment section: (1) sidebar green check + PRMS SYNC gate stayed stale after save — `patchAlignment` never refreshed `cache.greenChecks()` (only `ToPromiseService`'s `loadingTrigger` finalize hook does, and no PATCH goes through it); (2) Back was a dead button — `[showBack]="true"` rendered with no `(back)` binding, and `NavigationButtonsComponent` only emits an `@Output`. Back now navigates to the section above (ip-rights for indicator_id 1/2, else evidence), preserves `version`, and never saves | client/research-indicators/src/app/shared/services/bilateral.service.ts · .../pool-funding-alignment/pool-funding-alignment.component.ts · .../pool-funding-alignment.component.html · + 2 co-located specs | 7 new regression tests; full client suite `npm test -- --silent` **311/311 suites, 6515/6515 tests** green; `npx eslint` 0 errors on the 2 production files (spec files are outside the eslint config — declared limitation); K-004 mutation probe: removing the `(back)` binding and the `updateGreenChecks()` call reddened **4** of the new tests by name | [SPEC:quick/pool-funding-post-save-wiring] |
| 2026-08-25 | quick/pool-funding-orphan-card-and-validation-messages — **Triviality Gate FAILED (behavior change, bugfixes); fast-tracked on explicit user authorization.** QA triage: of 4 claims in QA's "Bug 1", **only 1c was a real defect** — the read-only ToC card survived a *deselection* because `readOnlyTocSummaries` fed off the server snapshot and never consulted `formData()`, contradicting the removal modal's "will discard that alignment"; R-BIL-129 modelled only not-Primary and stale, never "SP no longer selected". Fixed by gating the union on the current selection. **QA's 1a (delete ToC on demotion) NOT actioned — it is R-BIL-129 AC.1 + R-BIL-125 as specified, and inverting it is OQ-3, owned by the BA (A-3: would foreclose the deferred PRMS inbound story). Escalated, not fixed.** 1b/1d were save-based-form misreadings, documented. QA Bug 2 (real): `canSave()` blocks on Yes + zero SPs while `primaryRequiredMessage` returns null in exactly that case → added `spRequiredMessage`. **Bug 3 found during triage, unreported by QA:** `primaryRequiredMessage` was computed and unit-tested but rendered in NO template, so R-BIL-127 AC.3 was unmet in the UI — now rendered. Both messages placed after the picker branch chain, gated on `showSpPicker()`, so one copy serves single- and multi-SP and stays silent on unmapped/stale/no-SPs | client/research-indicators/src/app/pages/platform/pages/result/pages/pool-funding-alignment/pool-funding-alignment.component.ts · .component.html · + co-located spec | 7 new regression tests, all asserted in the DOM (Bug 3 existed because the computed was tested while never rendered); full client suite `npm test -- --silent` **311/311 suites, 6521/6521 tests** green; `npx eslint` clean on the component. K-004, two probes: (A) removing all three fixes reddened exactly the 3 owning tests; (B) over-fixing 1c to hide every read-only card reddened the OQ-3 guard test plus the pre-existing R-BIL-129 AC.1/AC.5 and stale-snapshot tests — the boundary is pinned in both directions | [SPEC:quick/pool-funding-orphan-card-and-validation-messages] |
| 2026-08-25 | quick/orphaned-toc-card-explains-itself — **Triviality Gate PASSED** (copy-only; one static string + one `@if` on `row.isOrphaned`, a flag already gating the sibling tag in the same element; existing `--ac-grey-600` token; no logic, state, data, or contract change). The read-only card for a demoted (non-Primary) SP stated WHAT it was ('Not the current Primary — read-only') but never WHY it was still on screen, so three independent reviewers — QA Bug 1a, twice, plus a product review — each read deliberately retained data as a defect. Surfaces R-BIL-129's own rationale ('data that is still stored — and still bound for PRMS — does not vanish from the screen'), which had never appeared in the UI. **Does NOT touch OQ-3**: the row, its retention, and the demotion cascade are unchanged; only the explanation is added. Rendered for `isOrphaned` only, never for the stale case. NOT done here and flagged instead: the amber `.pf-stale-tag` treatment (`--ac-orange-1`) is shared with the genuine stale warning and reads as 'something is wrong' — recolouring the orphan case is a token decision and belongs in docs/ux-ui/design.md, not a quick change | client/research-indicators/src/app/pages/platform/pages/result/pages/pool-funding-alignment/pool-funding-alignment.component.ts · .component.html | full client suite `npm test -- --silent` **311/311 suites, 6521/6521 tests** green (no existing assertion broken); `npx eslint` clean. Render proven with a throwaway probe, not asserted on faith: adding `toContain(ORPHANED_TOC_EXPLANATION)` to the existing R-BIL-129 AC.1 test passed, and removing the template block reddened it — probe reverted, no new test committed (per the command's no-new-suite rule) | [SPEC:quick/orphaned-toc-card-explains-itself] |
| 2026-09-17 | quick/create-result-version-param — **Triviality Gate FAILED (behavior change, bugfix); fast-tracked on explicit user authorization instead of `/akili-propose`.** Creating a result while standing on a **versioned** result (`/result/STAR-19972/general-information?version=2026`) left every subsequent call for the NEW result carrying `reportYear=2026`, and a just-created result has no versions, so `metadata`, `general-information` and the other section GETs failed. **Mechanism:** `resultInterceptor` reads `version` straight off `router.url` at request time (`getYearFromUrl`), and `successRequest` sets `cache.currentResultId` to the new result BEFORE `router.navigate`, so `ResultComponent`'s metadata effect and every section's `versionWatcher.onVersionChange(() => this.getData())` fire while the ORIGIN url is still active. Fixed with an explicit one-writer flag: `CacheService.skipResultVersionParam`, raised by the create flow only for the span of the navigation and cleared in `.finally()` so a rejected navigation cannot leave it up; the interceptor withholds the year while it is raised. **TWO earlier approaches were implemented, measured and fully REVERTED, recorded here rather than dropped:** (1) reordering `successRequest` to set the cached id after the navigation resolves — fixed `metadata` ONLY, because each section component fires its own request off its own effect; the user caught this in live testing and correctly asked for one general fix instead of per-request patches. (2) a guard in the interceptor comparing the result id in `router.url` against `cache.getCurrentNumericResultId()` — general and green across the whole suite, but it changes behaviour on EVERY request behind `X-Use-Year`, and the user's own question (*results-center → a result version*) exposed a case I had not verified: a stale `currentResultId` during that entry would suppress a year that was legitimately needed. Reverted on the user's explicit call to keep the blast radius at the create flow. **Scope, provable:** `grep skipResultVersionParam` over production code returns 5 lines in 3 files, with exactly ONE writer (`create-result-form.component.ts:386`), reachable only via the single `createResult(true)` caller in the template, only on `result.successfulRequest`, and only on the STAR branch. Outside that window the flag is `false` and the interceptor behaves byte-identically to before. | client/research-indicators/src/app/shared/services/cache/cache.service.ts · .../shared/interceptors/result.interceptor.ts · .../create-result-modal/components/create-result-form/create-result-form.component.ts · + 3 co-located specs (`api.service.spec.ts` mocks updated because the interceptor now injects `CacheService`) | 5 new tests (flag raised during navigation · cleared on resolve · cleared on REJECTED navigation · never raised for a non-STAR result · interceptor withholds/sends per the flag). **Gates proven able to fail BEFORE being cited (K-004), both sides:** removing the interceptor guard reddens 1 test, removing the component flag reddens 2; restored and re-measured green. Full client suite `npm test -- --silent` **323/323 suites, 7270/7270 tests** green · `npm run build` exit 0 (only pre-existing NG8102/NG8112 warnings in untouched files) · `npx eslint` clean on the 3 production files · `tsc -p tsconfig.spec.json` compared as a normalized before/after SET per touched spec: `create-result-form.component.spec` 68→68 and `api.service.spec` 6→6, **no new errors**. **Scope limits (KZ-017):** jsdom issues no real navigation and no real HTTP, so the tests assert the flag's lifecycle and the interceptor's branch — NOT the actual race between Angular's effect flush and the router, which is the thing that produces the bug. The race itself was reasoned from the code and never observed failing in a running app; the fix is confirmed by human testing only. **Not fixed, deliberately:** `reportingPlatforms` is derived from the same possibly-stale `router.url` (`getPlatformFromUrl`) and carries the identical latent defect — left alone because both results were STAR in the reported case and that function carries a comment recording a past AICCRA regression, so it deserves its own measurement rather than a change made in passing. | [SPEC:quick/create-result-version-param] |
| 2026-09-18 | quick/toc-levels-innovation-use — **Triviality Gate FAILED (behavior + write-gate change); fast-tracked on explicit user authorization instead of `/akili-propose`, because the change cannot be tested locally and needs to reach Dev.** User reported that the Pool Funding section renders incompletely for **Innovation Use** while a Capacity Sharing result (screenshot said "innovation dev", the screenshot itself is `CAPACITY SHARING FOR DEVELOPMENT` — same `['OUTPUT']` row either way) renders the full "Map Theory of Change results and indicators" block, and suspected a client-side validation. **It is not a client validation: the client has no result-type gate anywhere.** `showTocBlocks()` is `allowedLevels().length > 0` and `allowedLevels` is read straight off the server envelope's `allowed_levels`. The rule is server-side and keyed on `results.indicator_id` → `resolveResultTypeKey()` → `allowedLevelsFor()`; `innovation_use` simply had **no row** in `ALLOWED_LEVELS_BY_RESULT_TYPE`, so it fell through to `[]` ⇒ `catalogs: []`, zero upstream lambda-toc calls, and the FE hid the block. **This was the documented intended state, not a bug**: archived `requirements.md` row *"anything else (pending OQ-V2-5) → `[]`"*, and **OQ-V2-5** (*"Level rules / visibility for other result types"*, owner BA via Juanca, target pre-GA) was still open. **The change resolves OQ-V2-5 for `innovation_use` only** — level chosen by the user as **`OUTCOME`** (no exact precedent; `policy_change` uses OUTCOME but paired with EOI). `knowledge_product`, `oicr` and `unknown` stay `[]` and OQ-V2-5 stays open for them. **One line in the rule table is the whole production change**, because that table is the single source of truth (D-V2-3) feeding BOTH the catalog read (`bilateral.service.ts:372`) and the alignment write gate (`bilateral.service.ts:1125`) — read and write activate together and cannot diverge (R-BIL-091 AC.3). **Zero client changes**: `TOC_TYPE_MATRIX.innovation_use` and `TYPE_BADGE_LABELS['Innovation Use']` already existed in `indicator-type-guidance.util.ts` and were **unreachable dead code** (`classifyIndicator` is called only from `sp-toc-alignment-block`, which renders only inside `@if (showTocBlocks())`) — they come alive on their own. No upstream work either: the lambda-toc call is `/category/{level}/initiative/{sp}`, keyed on level + SP only, so the indicator never travels upstream and the OUTCOME catalog for those SPs already exists. **Known behavioural consequence, flagged to the user before commit and NOT a side effect to discover later:** `pool-funding-alignment.component.ts:386` skips the save gate when `showTocBlocks()` is false, so Innovation Use results on version 2026 previously saved with only an SP selected; the Primary SP's ToC draft is now **required to save** the section. Pre-existing 2026 results with pool funding saved but no ToC will be asked for the mapping on their next save. Older versions are unaffected (`versionLocked` disables the gate). | server/researchindicators/src/domain/entities/bilateral/utils/toc-level-rules.util.ts · …/toc-level-rules.util.spec.ts | **Gate proven able to fail BEFORE being cited (K-004):** deleting the `innovation_use` row reddens exactly the new test, by name, with `Expected ['OUTCOME'] / Received []` at `toc-level-rules.util.spec.ts:72`; restored from backup and re-measured green. The pre-existing assertion `allowedLevelsFor('innovation_use')).toEqual([])` was **retargeted, not deleted** — it moved out of the "any other type → []" group into its own positive `it`, and the remaining four cases (`knowledge_product`, `oicr`, `unknown`, `''`) still guard the fall-through. `npx jest --testPathPattern=bilateral` **19/19 suites, 347/347 tests**; full server suite `npm test -- --silent` **388/388 suites, 3322/3322 tests** green; `npx eslint` (no `--fix`, per K-001) exit 0 on both files. **Scope limits (KZ-017):** every test here is **mocked** — nothing run proves lambda-toc actually returns OUTCOME-level ToC results for the SPs in play. If PRMS holds no OUTCOME entries for a given SP the section will now render **but empty** (`toc_results: []`; the envelope keeps the level entry rather than filtering it, R-BIL-090 AC.5), which is indistinguishable here from working. `TocIntegrationService` caches per `(sp, level)` for **5 minutes** and the `OUTCOME` key has never been requested, so first load is cold: a lambda-toc outage surfaces as **503**, not as an empty section. `npm test` (`rootDir: "src"`) does not run `test:e2e`, `test:integration` or `test:fixtures`; those were checked by grep only (no reference to `allowedLevelsFor` / `allowed_levels` / `toc-level-rules` under `test/`) — a grep, not a run. The client suite was not run because no client file changed. **Not done, deliberately:** no spec folder was created. This resolves OQ-V2-5 for one result type and that resolution currently lives only in this row — it belongs in a Lite spec under `docs/specs/bilateral/toc-levels-innovation-use/`, which is pending. | [SPEC:quick/toc-levels-innovation-use] |
| 2026-09-18 | quick/pool-funding-year-gate — **Triviality Gate FAILED (behaviour change: a section can now be hidden that previously rendered); fast-tracked on explicit user authorization — the user's standing instruction this session is that there is no time for `/akili-propose`.** The Pool Funding block in the result sidebar (the `OPTIONAL` divider, the *Pool funding alignment* item and the `PRMS SYNC` button) was gated on the **primary contract** only. The user asked for a **second** gate on the **reporting year**, and asked FIRST whether a parameter for "the year a result can be reported to PRMS" exists — with the explicit instruction not to invent one if it did not. **Reported before any edit: it does not exist.** `app_config` holds exactly two keys (`ARI_CLARISA_API_KEY`, `ARI_CLARISA_PROJECTS_PHASE`); the only year-shaped ENV var is `ARI_CLARISA_PROJECTS_PHASE`, whose semantics are *which CLARISA phase to pull projects from*, not *which year is reportable*; and **`report_years.is_active` is NOT a reporting window** — it is the `AuditableEntity` soft-delete flag (`DEFAULT 1`), and `getAllReportYears` returns every `is_active = TRUE` row across a ±7-year range, so many are active at once. `ReportYearService.activeReportYear()` (`report-year.service.ts:23`) does `findOne({is_active: true})` with no ordering, would return an arbitrary row, and has **zero callers — dead code**; recorded here so the next reader does not adopt it. The live year is the constant `MAPPABLE_LIVE_VERSION = 2026` (`toc-level-rules.util.ts:31`), whose own comment says it is *"intentionally a constant, not an env var, per the handoff"* (D-V2-7). **The user then redirected the search to the client**, recalling that the pool funding form already goes read-only off-2026 — correct, and it is `versionLocked` (`pool-funding-alignment.component.ts:268`), but that is **not a client parameter either**: both its sources are the server (`tocCatalog()?.version_locked`, and a 409 `toc_mapping_version_locked`). A sweep of the whole client for `2026` found it only in two **copy strings** (`:168` banner, `sp-toc-alignment-block.component.ts:179` callout — both will read wrong when the year rolls and neither drives logic) and in the **admin** bilateral-mapping module (`phase = signal(2026)`, a different concept). **So nothing was created.** The server already emitted `version_locked` on the alignment payload (`bilateral.service.ts:655`, inside `getAlignment` — the exact endpoint the sidebar consumes) and the client stores the response unmapped (`bilateral.service.ts:168` `currentAlignment.set(res.data)`); it was simply **undeclared** on `AlignmentResponse`. The change declares it and adds one condition. **Placed at the single existing choke point** `shouldHidePoolFundingTab()` rather than at the three call sites, because the button reads `hasPoolFundingOption()` and the divider reads `option.optional`, both derived from that one filter — gating them separately is how they drift apart. **Compared with `=== true`, deliberately FAIL-OPEN:** a server that omits the field leaves the section visible (today's behaviour) instead of hiding Pool Funding for every result. | client/research-indicators/src/app/shared/components/result-sidebar/result-sidebar.component.ts · …/shared/interfaces/bilateral/pool-funding-alignment.interface.ts · + co-located spec | 5 new tests (hidden on `version_locked: true` despite `eligible: true` · **button + divider** both gone, asserting `hasPoolFundingOption() === false` AND the `[data-testid="sidebar-optional-divider"]` node is absent, so the gate is proven to reach all three surfaces the user named, not just the sidebar item · visible on `eligible: true` + `version_locked: false` · **fail-open** when the field is absent · both gates true together). **Gate proven able to fail BEFORE being cited (K-004):** replacing the new `return alignment.version_locked === true;` with `return false;` reddens exactly **2** tests, named — *"hides the Pool Funding alignment tab when version_locked=true, even though eligible=true"* and *"hides the PRMS SYNC button and the OPTIONAL divider when version_locked=true"*; restored from backup and re-measured green. `result-sidebar` suite **127/127**; full client suite `npm test -- --silent` **323/323 suites, 7275/7275 tests**; `npm run lint -- --quiet` → "All files pass linting"; `npx ng build --configuration development` exit 0. **Confirmed working by the user in the running app.** **Scope limits (KZ-017):** the year comparison itself is **never executed here** — it runs on the server and the client only consumes the boolean, so nothing in this suite proves 2026 is the right year or that the server computes it correctly; jsdom also paints nothing, so the divider assertion is DOM-presence, not layout. No server test ran (no server file changed). **Known consequence, flagged to the user before commit:** results on older versions previously rendered Pool Funding **read-only** and now do not render it at all, so saved alignment data on a 2025 version becomes invisible in STAR. The user accepted this. **Known follow-up, NOT fixed here:** the sidebar reads `bilateralService.currentAlignment()`, refreshed only by `getAlignment()`, so changing the year (General Information) or the primary contract (Alignment) does not re-evaluate visibility until a page reload — user-reported immediately after this shipped; tracked as its own change. | [SPEC:quick/pool-funding-year-gate] |
| 2026-09-18 | quick/pool-funding-visibility-refresh — **Triviality Gate FAILED (behaviour change: two section saves now issue an extra request); fast-tracked on explicit user authorization, same standing instruction as the row above.** Immediate follow-up to `quick/pool-funding-year-gate`: with both gates live, the user found that **changing the reporting year or the primary contract did not re-evaluate visibility** — the Pool Funding block kept its pre-save state and only corrected on a full page reload. **Mechanism:** both gates read `BilateralService.currentAlignment`, and the only thing that populated it was `result.component.ts:57`, whose fetch is memoized on `` `${alignmentCode}?version=${version}` `` — the code and the **URL** version. Editing the year inside the General Information form changes `results.report_year_id`, not the URL, and editing the contract changes neither, so the memo key was identical on every re-run and `getAlignment` was never re-issued. The two saves are the only places those two inputs change (user's own framing, confirmed in the code: the year via `PATCH_GeneralInformation`, the contract via `PATCH_Alignments`), so the refresh goes there rather than into the memo — widening the memo key would have meant inventing a trigger for a value the memo cannot observe. **Both call sites re-read the alignment only after the save SUCCEEDED**: in General Information inside the `response.successfulRequest && response.status !== 409` branch, after `metadata.update()`; in Alliance Alignment at the end of `patchAlignmentAndReload`, which already carries an early `return` on `!successfulRequest`. **The helper swallows its own failure by design** (identical private `refreshPoolFundingVisibility()` in both components, `try/catch` with a doc comment): the PATCH has already committed server-side, so a failed refresh must leave the stale-but-harmless previous visibility rather than turn a successful save into an error toast or a rejected promise. Neither component injected `BilateralService` before; both now do. | client/research-indicators/src/app/pages/platform/pages/result/pages/general-information/general-information.component.ts · …/alliance-alignment/alliance-alignment.component.ts · + both co-located specs | 6 new tests, **3 per component and deliberately symmetric**: re-reads after a successful save (General Information additionally asserts the exact argument, `'123'`) · does **NOT** re-read when the save failed · the save still resolves and still shows the success toast when the refresh **throws**, which is the clause the `try/catch` exists for and the one a happy-path-only test would leave unguarded. **Gate proven able to fail BEFORE being cited (K-004):** replacing both `await this.refreshPoolFundingVisibility();` calls with a comment reddens exactly **2** tests, one per component, by name — *"re-reads the pool funding alignment after a successful save, so the sidebar re-evaluates visibility"* in `GeneralInformationComponent` and in `AllianceAlignmentComponent`; restored from backup and re-measured green. Affected suites **196/196**; full client suite `npm test -- --silent` **323/323 suites, 7281/7281 tests** (7275 + the 6 added); `npx tsc -p tsconfig.app.json --noEmit` exit 0; `npm run lint -- --quiet` → "All files pass linting"; `npx ng build --configuration development` exit 0. **Both cases confirmed working in the running app by the user** (year changed in General Information, contract changed in Alliance Alignment — the block now hides/shows without a reload). **A flaky full-suite run is recorded rather than hidden:** the first `npm test` reported *1 failed suite / **0** failed tests* and a test count 7 short; a clean re-run gave 323/323 and 7281/7281. That is the known client-runner flake (failed suite with zero failed tests), not a regression — re-run before investigating. **Scope limits (KZ-017):** jsdom performs no real HTTP and no real navigation, so these tests assert that `getAlignment` is **called at the right moment with the right argument** — never that the sidebar repaints, never that the server returns different `eligible`/`version_locked` values after the save. The end-to-end behaviour is human-verified only. The refresh is also **not** wired to any other writer of those two fields: anything that changes `report_year_id` or the primary contract outside these two forms (a direct API call, a bulk operation, PRMS inbound) still leaves the sidebar stale until reload — deliberately out of scope, since the user named these two saves as the places the variables change. | [SPEC:quick/pool-funding-visibility-refresh] |
| 2026-09-18 | quick/pool-funding-tag-in-contract-dropdowns — **Triviality Gate FAILED (a data-precedence defect had to be fixed for the visual to work at all); fast-tracked on explicit user authorization, same standing instruction as the two rows above.** The **CONTRIBUTING TO POOL FUNDING** tag existed in the projects table (`my-projects.component.html`) and the project card (`project-item.component.html`) but not in the contract pickers, so the same concept read differently depending on where you stood. Added to **all four** contract templates: the Create Result dropdown (`shared-result-form`, shared with Result AI Assistant) and, in Alliance Alignment, BOTH the dropdown panel row (`#item`) and the selected-project cards (`#rows`) — the cards deliberately included because that is where the **Primary** project is chosen and the Primary contract is exactly what gates the Pool Funding section (see `quick/pool-funding-year-gate`). **No new markup was invented:** same `app-custom-tag`, same `statusId="pool-funding"` (tokens `--ac-pool-funding-border` / `--ac-pool-funding-fg`, so dark mode comes free), same copy, same `[tiny]="true"` as the table, placed beside the contract status exactly as `project-item` does, with `shrink-0` so a long project title cannot squeeze it out. `is_pool_funding_contributor` was already on the wire (`agresso-contract.repository.ts` `getContracts` selects `effectivePoolFundingContributorSql(...) AS is_pool_funding_contributor`, `MappedContractsDto` maps it, `GetContractsService` spreads it) — it was merely undeclared on the client's `GetContracts` interface. **A REAL DEFECT was found and fixed on the way, without which the cards could never have shown the tag:** the flag has two forms — the catalog (`find-contracts`) returns the **EFFECTIVE** predicate (raw column **OR** an active `bilateral_project_mapping` row) while the alignment payload arrives through the TypeORM `agresso_contract` relation, i.e. the **RAW** `agresso_contracts.is_pool_funding_contributor` column (`default: false`). A contract that contributes *via its bilateral mapping* — the normal case, e.g. D514 — is therefore `true` in the catalog and `false` in the alignment payload, and `enrichPortfolio2Contracts`' `{...catalogMatch, ...flattened}` silently kept the false one. Fixed by pinning the field after the spreads, beside the `agreement_id` / `description` / `contract_id` / `is_primary` pins the function already carried for the same reason; catalog wins, raw is the fallback only when the catalog carries none. | client/research-indicators/src/app/shared/interfaces/get-contracts.interface.ts · …/shared/components/shared-result-form/shared-result-form.component.{html,ts} · …/result/pages/alliance-alignment/alliance-alignment.component.{html,ts} · …/alliance-alignment/portfolio/alliance-alignment-p2.component.{html,ts} · …/portfolio/portfolio-2-alignment.mapper.ts · + mapper spec | **4 new tests, all on the mapper — the only part of this change that is reachable by a test** (catalog `true` survives an alignment `false` ← the defect itself · survives an alignment that omits the field · a catalog `false` is NOT overridden into true · falls back to the alignment value when the catalog carries none). **Gate proven able to fail BEFORE being cited (K-004):** deleting the pin reddens exactly **1** test, the one covering the defect; the other three stay green without it, which is correct — only one of the four discriminates. Full client suite `npm test -- --silent` **323/323 suites, 7285/7285 tests**; `npx tsc -p tsconfig.app.json --noEmit` exit 0; `npm run lint -- --quiet` → "All files pass linting"; `npx ng build --configuration development` exit 0. **Confirmed working in the running app by the user** in the Create Result modal and in Alliance Alignment (dropdown and cards). **Scope limits (KZ-017), measured rather than assumed:** the four templates are **structurally unreachable** from the test suite and no test asserts the tag renders. Probed twice and both failures were observed, not reasoned: the `shared-result-form` panel renders its overlay and scroller in jsdom (`p-select-overlay: true`, `p-scroller: true`) but **zero option rows** (`A1080` absent) because `[virtualScroll]="true"` has no measurable viewport; and the multiselect's `selected-items` container renders **empty** (`Select as Primary` and `WB-IDA` both absent) even with `body().contracts` holding two entries. `ng build` cannot cover the gap either — `let-item` / `let-contract` are `any` in an `ng-template`, so `strictTemplates` type-checks nothing there. Both throwaway probes were deleted and the touched specs left clean. Dark theme unasserted; the tag inherits the same tokens as the three places that already ship it. **Two hours were lost to a wrong assumption worth recording:** the tag was first added to `alliance-alignment.component.html` and did not appear, and four rounds of debugging chased data (payload confirmed `true` in DevTools), build staleness (the live `ng serve` bundle was fetched and grepped — `chunk-GIYBRT3H.js` contained the compiled `ɵɵconditional`) and markup (an isolated TestBed host proved `app-custom-tag` renders inside that exact `<p>` + `@if`). **All three were fine; the template was simply never rendered.** `alliance-alignment.component.html:3` branches on `@if (isPortfolioP2Alignment())` and delegates to `<app-alliance-alignment-p2>`, a component with its **own** `#item` and `#rows`. The user's very first screenshot showed *Research Areas* and *Strategic Objectives* — fields that exist **only** on P2 — which identified the branch from the start and was not read. *Lesson: when a shipped template provably does not render, check whether the component delegates before checking anything else.* Both variants now carry the tag (`portfolio_id = 2` and `portfolio_id ≠ 2`), so the section does not change behaviour with the portfolio. **Not visually validated:** the Result AI Assistant, which shares `shared-result-form` with the approved Create Result modal. | [SPEC:quick/pool-funding-tag-in-contract-dropdowns] |
| 2026-09-18 | quick/prms-code-in-sidebar — **Triviality Gate FAILED (server change: a new field on the alignment contract); fast-tracked on explicit user authorization, same standing instruction as the rows above.** Requested: under the `PRMS SYNC` button, a grey line reading `PRMS code #…`, matching the `Result code #…` line at the top of the sidebar. **The value existed but never reached the client on a page load.** `results.prms_result_code` is written by the sync engine (`result-prms-sync.service.ts:257`, from the interpreter's `readResultCode`), and `GET results/{id}/prms-sync` exposes it — but the client has no `GET_PrmsSyncStatus` method at all, only `POST_PrmsSync`, so the code was visible exactly once, in the POST response, and lost on reload. **Chosen shape: no new HTTP call.** `findPoolFundingAlignmentContext` already does `SELECT … FROM results r`, and the sidebar already consumes the alignment payload for `eligible` / `version_locked` / `is_synced_to_prms`, so the column was added to that SELECT, to the context type, to the response and to its `@ApiProperty` — four small edits instead of a second round trip and a second piece of component state. **Copy and styling are the existing line's, not new ones:** same `#a9adb2`, same `space_grotesk`, same `tracking-[2.5px]`, same `!font-[200]`, same `#{{ }}` split, so the two read as one family. **Renders only when a code exists** — `null` (the normal state before a sync) and an omitted field both render nothing rather than a bare `PRMS code #` label, which is also what an older server produces. | server/researchindicators/src/domain/entities/results/repositories/result.repository.ts · …/bilateral/bilateral.service.ts · …/bilateral/dto/update-pool-funding-alignment.dto.ts · client/research-indicators/src/app/shared/interfaces/bilateral/pool-funding-alignment.interface.ts · …/components/result-sidebar/result-sidebar.component.{ts,html} · + 3 specs | 3 new client tests (renders `PRMS code #54321` with the exact text · renders nothing on `null` · renders nothing when the server omits the field). **Gate proven able to fail BEFORE being cited (K-004):** deleting the template block reddens exactly 1 test; restored from backup and re-measured green. **2 pre-existing server tests broke on their own and were EXTENDED, not deleted** — both are exhaustive shape assertions over `AlignmentResponse` (`bilateral.service.spec.ts` deep-equals the whole object, `bilateral.controller.spec.ts` enumerates every `@ApiProperty` field), so they caught the new field immediately and correctly; that is direct evidence the response contract is genuinely covered. Client suite **335/335 suites, 7782/7782 tests**; server suite **413/413 suites, 3894/3894 tests**; `npx tsc -p tsconfig.app.json --noEmit` exit 0; `npm run lint -- --quiet` → "All files pass linting"; `npx eslint` (no `--fix`, per K-001) exit 0 on the three server files; `npx ng build --configuration development` exit 0. **Scope limits (KZ-017):** jsdom paints nothing, so the tests assert the element and its text, never that the line sits under the button or matches the top line visually — human-verified only, and **not yet visually confirmed** (it needs a result that has actually synced, since `prms_result_code` is null otherwise). No server test exercises the new SELECT against a real database; the column's presence on `results` is read off the migration and the entity, not off a live query. Dark theme unasserted (the colour token is the existing line's). ⚠️ **Needs a BACKEND deploy, not just the client** — the field does not exist on the current Dev API response. | [SPEC:quick/prms-code-in-sidebar] |
| 2026-09-18 | quick/unblock-innovation-use-prms-send — **Triviality Gate FAILED (removes two layers of server-side refusal from the PRMS send path); fast-tracked on explicit user authorization.** Syncing an Innovation Use returned `REFUSED_BY_STAR` / 422 with *"Innovation Use is gated: STAR holds no investment declarations (usd_budget / is_determined)"*. **The user asked whether that was ours or PRMS's, and to remove it if ours, because the team needed to learn whether PRMS actually requires those fields.** It was ours, twice over. **(1) The gate.** `sync-gate.ts`'s `indicator_not_gated` entry refused EVERY Innovation Use: its predicate was `snapshot.indicator_id === IndicatorsEnum.INNOVATION_USE` and **never read `usd_budget` or `is_determined`**, despite naming them in its own description — a placeholder pending a PRMS PO meeting, dressed as a validation. Because STAR refused locally, PRMS never saw a payload, so the question it was waiting on could never be answered. Removing it is exactly the operation the design specified: `design.md:202` keeps the gate as one enumerated list precisely so *"no gated type is expressed as a missing branch"*, and **QA-5** measures a lift as *"one list entry removed and zero builder files changed"* — which is what this was (the `SyncGateEntryId` union member went with it; no builder touched). `HANDOFF.md:126` already recorded Innovation Use as *"built but gated"*. **(2) The builder.** With the gate lifted the send reached `innovation-use.builder.ts`, which threw `Missing mandatory field 'actors'`. Two **empty-collection** throws (`actors`, `measures`) were removed — `measures` too, deliberately, because it is the identical wall on the next attempt and fixing one at a time burns a round trip each. The file's own header admits the shape *"follows D-B **BY ANALOGY**"* and that the type *"was never called in T-01"*: assumptions, never a confirmed contract. **Empty collections are now OMITTED, not sent as `[]`** — an empty array asserts *"we checked and there are none"*, a different claim from *"we are not declaring this"*, and the point was to let PRMS answer an unbiased question. **Per-row validation deliberately UNCHANGED** (`actor_type_id`, `other_actor_type`, `how_many`, `institution_types_id`): those fire only when a row EXISTS but is malformed, and a half-built row would make PRMS reject for the wrong reason. `innovation_use_level` also kept — without it the payload is not an Innovation Use at all. | server/researchindicators/src/domain/entities/result-prms-sync/eligibility/sync-gate.ts · …/tools/prms-normalizer/builders/innovation-use.builder.ts · + both specs | **Gates proven able to fail BEFORE being cited (K-004), both halves:** forcing the builder to always emit both keys (`{ actors, measures }`) reddens **3** tests; restored and re-measured green. Server suite `npm test -- --silent` **388/388 suites, 3326/3326 tests** (3322 before + 4 added; the 413/3895 figure in the row above was measured on `dev`, which carries the pi-delegates merge — not a regression); `npx tsc --noEmit -p tsconfig.json` exit 0; `npx eslint` (no `--fix`, per K-001) exit 0 on both production files. **3 pre-existing tests were RETARGETED, not deleted:** the gate's multi-type refusal test (Innovation Use now asserts `allowed: true` and that it reaches transport), the QA-5 lift test (the entry is gone from `SYNC_GATE_ENTRIES` itself, so the assertion is now direct), and the builder's measures test (its first half — `quantity: 0` survives, incomplete extras drop — was kept intact; only the throw became an omission assertion). **4 new tests:** no remaining gate entry refuses a type by indicator alone · `actors` omitted when empty · builds with **both** empty (`current_innovation_use_numbers: {}`) · **still throws** on a present-but-malformed row. **Scope limits (KZ-017):** nothing here is verified against PRMS — every test is a unit test over the builder and the gate list, and whether PRMS accepts the payload is by definition unknowable from this repo. That was the point of the change, not an oversight. **OUTCOME, measured live by the user immediately after:** PRMS returned a real JSON-Schema rejection — `keyword: "required"`, `path: "/innovation_use/current_innovation_use_numbers"`, `missingProperty: 'innov_use_to_be_determined'` — i.e. **`REJECTED_BY_PRMS`, not `REFUSED_BY_STAR`**. Two findings from that: **PRMS complained about NEITHER `actors` NOR `measures`**, so both removed refusals were genuinely unnecessary; and the original gate's `is_determined` was pointing at something real — PRMS's field is `innov_use_to_be_determined` (abbreviated `innov`, nested inside `current_innovation_use_numbers`) — but the gate blocked the whole type instead of checking it. **STAR does not hold that field for Innovation Use:** `result_innovation_use` has only `innovation_use_level_id` and `innovation_use_level_explanation`. The concept exists only for Innovation **Dev**, where `prms-response.dto.ts:237` `innovation_user_to_be_determined: boolean` is mapped on READ to `THIS_IS_YET_TO_BE_DETERMINED` / `USERS_HAVE_BEEN_DETERMINED`. **Its TYPE is unconfirmed** — the error says only that the property is missing, never that it is a boolean; boolean is inferred from that Innovation Dev sibling and is exactly the kind of assumption this change existed to stop making. **Open, not fixed here:** `detailedErrors` carried a single entry, and a schema validator that reports the first failure may be hiding more required fields behind it — do not assume this is the last wall. | [SPEC:quick/unblock-innovation-use-prms-send] |
| 2026-09-18 | quick/store-prms-phase-id — **Triviality Gate FAILED (schema change + a new field through the whole sync persistence path); fast-tracked on explicit user authorization.** A successful PRMS push returns the reporting phase the result was filed under, visible in the sync log's `externalApiResponse` as `version_id: 36` plus a nested `obj_version: { id: 36, phase_name: 'Reporting 2026', phase_year: 2026 }`. STAR discarded it. Now stored on `results.prms_phase_id`, beside `prms_result_code` and written by the **same UPDATE** on a successful sync, so the two can never disagree about which sync they describe. **The user proposed three steps (migration, entity, save); it is seven files, because the value has to travel** from the interpreter to the UPDATE: `PrmsSyncInterpretation` gained the field, the repository gained it on its input type AND in the SQL, and the service passes it. **`tsc` found four return sites** in the interpreter that build their literal from scratch instead of spreading `base` — each had to carry `prmsPhaseId: null` explicitly, which is exactly the kind of path a hand-search misses. **Four decisions, all the user's, recorded because none is self-evident from the code:** (1) **`version_id` wins, `obj_version.id` is the fallback** — PRMS sends the same number in both places, so precedence is pinned rather than left to object key order; (2) **only the id** — `phase_name` / `phase_year` are deliberately NOT stored, since the year is already ours (`MAPPABLE_LIVE_VERSION`) and a second copy can drift; (3) **no backfill** — rows synced before the column keep NULL forever, accepted because this is test data and the feature is not in production; (4) **server-only** — `PrmsSyncResponseData`, the DTO the client reads, is deliberately untouched (verified, not assumed). **An absent phase does NOT degrade the sync:** the outcome stays `ACCEPTED` with a null phase, and only the *result code* still carries `PRMS_RESULT_CODE_ABSENT` — a missing phase is not a reason to dirty a successful push. | server/researchindicators/src/db/migrations/1789761421000-addPrmsPhaseIdToResults.ts · src/db/migration-specs/1789761421000-addPrmsPhaseIdToResults.spec.ts · …/results/entities/result.entity.ts · …/tools/prms-normalizer/response/prms-sync-response.interpreter.ts · …/result-prms-sync/repositories/result-prms-sync-log.repository.ts · …/result-prms-sync/result-prms-sync.service.ts · + 2 specs | 9 new tests. Interpreter: reads `version_id` · falls back to `obj_version.id` · **prefers `version_id` when the two disagree** · leaves null without failing an ACCEPTED sync · parses a numeric string · null on a non-accepted outcome. Migration: nullable bigint with **no NOT NULL and no DEFAULT** asserted explicitly · drops on down · **passes no parameters and contains no `?` or `:word`**, guarding the `namedPlaceholders` trap that makes a migration unrunnable from the day it is written. **Gate proven able to fail BEFORE being cited (K-004), two probes:** replacing `readPhaseId(row)` with `null` reddens **4** tests; inverting precedence to read `obj_version.id` first reddens **1**, the test that pins it. Both restored and re-measured green. **A measurement error of my own is recorded rather than hidden:** the first run of probe 1 reported 0 failures, because the output was piped through `cat -v`, which mangles the multibyte `●` the grep was matching — the gate was fine, the measurement was not (same family as K-014: a filtered view of output is not the output). **1 pre-existing test updated, not deleted:** the repository's UPDATE-parameter assertion, now `[555, 36, 42]` with a note that **order matters** — these are positional `?` params, so a transposition writes the phase into `prms_result_code`; it also asserts the SQL actually contains `prms_phase_id = ?`. Server suite `npm test -- --silent` **389/389 suites, 3335/3335 tests**; `npx tsc --noEmit` exit 0; `npx eslint` (no `--fix`, per K-001) exit 0 on all five production files. Prettier flagged the migration's formatting and was applied with `--write` (a fixer, which measures nothing), then re-verified with bare `eslint`. **Scope limits (KZ-017):** **the migration has NOT been run — not against Dev, not against a scratch schema.** Its spec drives a mocked `QueryRunner` (`jest.fn()`) and only inspects the SQL string that *would* have been sent, so nothing here proves the `ALTER TABLE` executes; per K-006 the only sound gate for that is running it. `test:fixtures`, the one suite that starts a real MySQL, was not run either. **Consequence, and it is not cosmetic: the column does not exist in any database yet, so deploying this code before applying the migration makes the UPDATE fail on the first accepted sync.** Applying it is a separate, human-decided step against a shared database (K-015: the pipeline ships code, never migrations). | [SPEC:quick/store-prms-phase-id] |
