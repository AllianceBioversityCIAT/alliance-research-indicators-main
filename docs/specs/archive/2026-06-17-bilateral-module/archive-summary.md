# Archive Summary — Bilateral module (epic AC-1385)

> **⚠️ Whole-module archive.** This is NOT a single completed feature. The entire `bilateral-module/` spec area was archived on 2026-06-17 at the user's explicit, twice-confirmed direction, with the bulk of the epic still open. Everything below the "Accepted follow-ups" section is **unfinished work** preserved here, not delivered work. Generated by `/sdd-archive`.

---

## 1. Document control

| Field | Value |
| --- | --- |
| Spec area | `bilateral-module` — epic AC-1385 (US0–US7) |
| Branch | `AC-1594-bilateral-module-v2` (in flight, not merged) |
| Archived by | Leader (sdd-archive), at user direction |
| Archive type | **Whole-module** (parent spec + all sub-feature folders + reference material) |

## 2. Original spec path

`docs/specs/bilateral-module/`

Contents archived: parent `requirements.md` / `design.md` / `tasks.md`; `indicator-mapping/`; `pending-items/`; `jira-us/`; `prms-context/`; `frontend-handoff.md`; `frontend-data-model.md`. (The completed `toc-mapping-v2/` feature was archived separately on 2026-06-17 to `docs/specs/archive/2026-06-17-bilateral-module--toc-mapping-v2/` and is NOT inside this move.)

## 3. Archive date

2026-06-17

## 4. Final status

**Archived while predominantly INCOMPLETE — by explicit user override.** The parent task list shows **83 open `[ ]` tasks and 0 done**; 6 of 7 user stories (US1, US3–US7) are Open; `pending-items/` has a task still `[~] in progress` (staging/prod rollout queued). Only US2's `toc-mapping-v2` feature reached completion (T-01…T-09, archived separately) and even it has an open gated follow-up (T-10). This archive was performed against the Leader's recommendation to keep the module active; the user confirmed twice.

## 5. Requirements delivered

Requirement band: R-BIL-001…098, NFR-BIL-001…011. **Delivered subset** (via the separately-archived `toc-mapping-v2` feature, US2/AC-1594): R-BIL-090…097 fully; R-BIL-098 prepared (code cleanup T-10 pending). **Not delivered / partial:** the remainder of the band (US1 tagging R-BIL-01x; US3 indicators; US4 mapping; US5 PRMS push; US6 W3 sync; US7 SP/ToC sync) and the foundation/phase tasks T-00…T-14+ in the parent `tasks.md`. Treat this summary's delivery claims as limited to the toc-mapping-v2 feature only.

## 6. Files changed summary

Code shipped for this module lives on branch `AC-1594-bilateral-module-v2` and is documented in the per-feature execution logs:
- `toc-mapping-v2/execution.md` (archived separately) — T-01…T-09 commits `92001d43`…`cf59295d`, plus follow-ups `5aef25ae`/`263a0bd9`/`9caf9fd8`/`f3af8b2c`.
- `pending-items/execution.md` (in this archive) — T-15.x series (US1/US2 foundation, AGRESSO tag, SP-resolution chain, bilateral-project-mapping CRUD, partial-unique migration, admin SSR basename fix). Several `[x]` done; rollout `[~]` in progress.
- Parent `tasks.md` T-00…T-14+ — predominantly `[ ]` (planning state; not implemented at the module level).

No code is moved or changed by this archive (docs-only operation).

## 7. Test evidence summary

No module-level `test-report.md`. Evidence is per-feature:
- `toc-mapping-v2` — 284 suites / 1660 tests; coverage 80.2/71.5/80.9/80.0 (archived separately).
- `pending-items` — unit tests + live smokes recorded inline in its `execution.md` (T-15.1/10/11/13/14/15 etc.).
- Unstarted user stories (US1, US3–US7) have no test evidence — not built.

## 8. Validation summary

No module-level `validation-report.md`. Per-feature conformance was enforced by the JCSPECS Reviewer persona (recorded PASS verdicts in each feature's `execution.md`). No unresolved FAIL findings in completed features. The bulk of the module was never validated because it was never implemented.

## 9. Accepted warnings / follow-ups (ALL OPEN — archived as live debt)

Owner: Juanca / epic AC-1385 unless noted. **This is the substantive content of this archive — open work, not closed.**

1. **Parent `tasks.md`: 83 open `[ ]` tasks** across Phase 0 foundation (T-00…T-06), Phase 1 US1+US2 (T-07…T-14), Phase 2 US3+US4 (T-15…T-16+), and later phases. Not implemented at the module level.
2. **Open user stories** — US1 (tag bilateral projects, AC-1438), US3 (ToC indicators, AC-1439), US4 (map results→indicators, AC-1440), US5 (push to PRMS, AC-1441), US6 (sync W3 registry, AC-1593), US7 (sync SPs/ToC, AC-1595). All marked Open in `jira-us/`.
3. **`pending-items/` rollout `[~]` in progress** — dev leg complete; staging + prod queued per `pending-items/rollout-checklist.md`. Includes a migration step and sign-off blocks not yet executed.
4. **`indicator-mapping/` open questions** — OQ-IM-1 (PO decision brief present), OQ-IM-3, and live `result_pool_funding_indicator_mapping` type-handler machinery (current behavior, not superseded).
5. **toc-mapping-v2 carried follow-ups** (detail in that feature's archive-summary §9): T-10 PRMS-pair deletion (gated, not done); RB-1 lambda-toc DNS (infra); RB-4 FE relay to STAR; OQ-V2-2/3/5/6 BA decisions.
6. **Branch not merged** — `AC-1594-bilateral-module-v2` carries the delivered toc-mapping-v2 code; merging/cutover still pending.

## 10. Historical notes

- **Why this archive is unusual:** `/sdd-archive` targets a single completed feature (`<module>/<feature>`). This invocation moved the whole module container while the epic was ~mostly unstarted. The Leader flagged the blast radius (83 open tasks, 6 open user stories, in-progress rollout) and recommended against it; the user chose "archive the whole module anyway" and confirmed a second time on an explicit final gate.
- **To resume any bilateral work:** the specs for US1/US3–US7, the pending-items rollout, and indicator-mapping now live under `docs/specs/archive/2026-06-17-bilateral-module/`. Restore with `git mv` back to `docs/specs/bilateral-module/`, or reference them in place.
- **iCloud hazard (from the 2026-06-10 session):** the repo sits under iCloud-synced `~/Desktop`; it produced conflict duplicates and a hung `dist/`. Durable fix: move the repo out of `~/Desktop` or disable Desktop iCloud sync.
- The completed **toc-mapping-v2** feature (US2/AC-1594) has its own richer archive at `docs/specs/archive/2026-06-17-bilateral-module--toc-mapping-v2/`.
