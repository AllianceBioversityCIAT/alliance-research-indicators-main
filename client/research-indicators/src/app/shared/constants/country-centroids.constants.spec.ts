import { PROJECT_DASHBOARD_DEFAULT_LIMIT } from './country-centroids.constants';

describe('country-centroids constants', () => {
  it('should export PROJECT_DASHBOARD_DEFAULT_LIMIT with value 5', () => {
    expect(PROJECT_DASHBOARD_DEFAULT_LIMIT).toBe(5);
  });
});
